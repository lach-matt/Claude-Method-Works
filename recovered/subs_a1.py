# Ruling 46 Amendment 1 + Ruling 60 — Register entry bodies, batch 1 (sites 1-44)
S1 = [
("**densities.py* AND *numbers-index.py*", "**the six densities* AND *the numbers index*"),
("*appendix_audit.py adds eight checks", "*Eight checks are added"),
("**`appendix_audit.py` FOUND ONE REAL FAULT AND THREE OF ITS OWN.**", "**THE EIGHT CHECKS FOUND ONE REAL FAULT AND THREE OF THEIR OWN.**"),
("*Generated by `indices.py`, four pages", "*Generated, four pages"),
("and `spectra.py` still read the book", "and the Compendium was still generated from the book"),
("*`channels.py` turns collected levels into channels", "*Collected levels are turned into channels"),
("**`channels.py` was never told it is hydrogenic**", "**the hydrogenic case was never distinguished**"),
("*`channels.py` now carries a limit only where one was published", "*the compendium now carries a limit only where one was published"),
("*`channels.py` splits a configuration string on a letter and divides; it is told nothing about what \u2113 means.*", "*The defect is got by splitting a configuration string on a letter and dividing; what \u2113 means never enters.*"),
("*Nothing in `channels.py` is told what \u2113 means", "*Nothing behind the channel table is told what \u2113 means"),
("`channel_verify.py` and `isoelectronic.py` regenerate all of it in two commands.", "all of it is regenerated in two commands."),
("*Two patterns in `compendium_audit.py` counted", "*Two patterns counted"),
("*`channel_verify.py` asked whether two defects agreed", "*The check asked whether two defects agreed"),
("*`mathreg.py` held fifteen", "*The mechanism list held fifteen"),
("**`mech_audit.py` now compares the three and exits non-zero on disagreement.**", "**The three are now compared, and disagreement fails the build.**"),
("*`channels.py` groups by (core, orbital, term, J) with n absent from the key", "*Channels are grouped by (core, orbital, term, J) with n absent from the key"),
("**`channels.py` WROTE THE BRACKET COLUMN AS", "**THE BRACKET COLUMN WAS WRITTEN AS"),
("and `channels.py` supplies neither.*", "and neither was supplied.*"),
(", and `power.py` cannot tell them apart from the counts alone.*", ", and the counts alone cannot tell them apart.*"),
("and it is a rewrite of `channels.py` rather than a collection.**", "and it is a rewrite of the channel table rather than a collection.**"),
("*\u2014 not a function in `mathverify.py`, not a function anywhere in the source tree.*", "*\u2014 not a check anywhere on record.*"),
("*`mathverify.py` defines sixteen functions and makes **103 rec() assertions", "*Sixteen checks are defined, making **103 assertions"),
("*`check_audit.py` classifies every field and can be run on any build.*", "*Every field is classified, and the classification can be recomputed on any build.*"),
("CHANNEL: `channels.py` USES R\u221e", "CHANNEL: THE CHANNEL TABLE USES R\u221e"),
("its absence from `channels.py` until today", "its absence from the channel table until today"),
("rewritten by every `channels.py` run.", "rewritten on every regeneration."),
("`pjj.py` is register 818, `converge_selfsame.py` is 811, `coreblind_fill.py` is 845", "the P.jj evidence is register 818, the self-same convergence is 811, the core-blind filling is 845"),
("and `mathverify.py` touches 46.**", "and the verification touches 46.**"),
("and `mathverify.py` checks none of them.**", "and none of them is checked.**"),
("**`pverify.py` IS WRITTEN, AND ITS FIRST VERSION WAS WRONG", "**THE P-FAMILY VERIFICATION IS WRITTEN, AND ITS FIRST VERSION WAS WRONG"),
("**`pverify.py` DID WHAT IT WAS BUILT FOR.**", "**THE P-FAMILY VERIFICATION DID WHAT IT WAS BUILT FOR.**"),
("*No audit read `mathreg.py` as a GRAPH", "*No audit read the object list as a GRAPH"),
("so a `mathreg.py` that did not load reported ALL TWENTY-FIVE PASS.**", "so an object list that did not load reported ALL TWENTY-FIVE PASS.**"),
("*`register_gen.py` matched entry labels with", "*Entry labels were matched with"),
("Stored as `ground.py`.**", "Stored in the ground-configuration table.**"),
("Saved as domain_protocol.py.*", "Recorded as the domain protocol.*"),
("*Saved as chem_index.py.*", "*Recorded as the chemical index.*"),
("the same as ground.py's:", "the same as the ground-configuration table's:"),
("`contingency.py`, four questions:*", "The contingency test, four questions:*"),
("**`ladder_index.py` carried it for twenty-eight registers", "**the ladder index carried it for twenty-eight registers"),
]