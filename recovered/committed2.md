# Committed before computation — B.2.13, session continuation

ROW 1 — the l-monotonicity condition
D1. delta_l decreasing in l is the whole condition; T is then order-reversing on (n,l).
D2. The l-axis has its OWN admissibility bound, of B.2.2 form, i.e. r ~ nu^-3.
    Committed shape: r_l = 2 R d(delta) / (nu^3 sigma) >= 5,
    i.e. B.2.2 with Z^2 replaced by Delta-delta.
D3. Therefore the l-axis exits the domain EARLIER than the n-axis by a factor
    (Z^2/Delta-delta)^(1/3) in nu. For Na d-f this is ~4.
D4. The tightening measured last session concentrates at high l, i.e. exactly
    where Delta-delta is smallest — so the gain is largest where the bound bites first.

ROW 2 — the spherical cavity
S1. Recoding m' = m + l, l' = l - 1 gives cap m' <= 2l' + 2 — SINGLE-argument.
    The criterion therefore predicts Lambda_sph IS a lattice: meet- and join-closed.
S2. Distributive, rank-modular, E(X) = 0, constraint graph acyclic.
S3. F(-1) = 0 exactly, because the TE/TM axis is a free 2-chain and (1 + (-1)) = 0.
    Committed as structural, not numerical coincidence.
S4. The bracket lives only on the (l, n) sublattice: m' carries multiplicity,
    not frequency. So the lattice is 4-coordinate but the bracket geometry is 2-D.
