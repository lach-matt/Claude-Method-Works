# Ruling 46's remaining entry-body mass: build number + the 'at build' lines
S4 = [
('The press stamps "Build 6" from a string', 'The press stamps a build label from a string'),
("*It is now generated from the headings at build time and cannot disagree.*", "*It is now generated from the headings and cannot disagree.*"),
("Two are now recomputed at build; \u00a732.1.4.1 names three more", "Two are now recomputed; \u00a732.1.4.1 names three more"),
("E(G) now recomputes at build rather than printing a failure notice.", "E(G) now recomputes rather than printing a failure notice."),
("**All six build-time readouts run for the first time", "**All six readouts run for the first time"),
("the artefact's numbers are computed at build time rather than typed", "the artefact's numbers are computed rather than typed"),
("Five of the six are now READ FROM THE FILE at build time (NCELL", "Five of the six are now READ FROM THE FILE (NCELL"),
("is read from the data companion at build and gated against it", "is read from the data companion and gated against it"),
]