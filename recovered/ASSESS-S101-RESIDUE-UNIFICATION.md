# ASSESSMENT S101 -- SERIOUS RESIDUE SURVEY AND UNIFICATION (M's directive)
## SEVERITY LINE: no new faults. One structural finding: the residue field is ONE root object with three
##   exposures, two of which are already closed or reducible to the third.
## 1. FULL OPEN-STATE ENUMERATION (archive-read, not memory):
##   (i)  G5b (F93.6 s93 + d-row (a),(b) merged s100): fractional-q d-shell non-stationarity at 58-B,
##        90-A, 91-A. THE one counted residue. One specified run (Item 1, prediction 56c319fc).
##   (ii) Carried faults F95.1-3, F95.5-6, F96.2, F96.4, F97.2-3, F98.3: ALL are SCORED honest-record
##        entries (falsified predictions with mechanisms, caught errors, comparison-decided specs).
##        NONE carries an open mathematical object. They are ledger, not residue. No action.
##   (iii) Order items 3 (G4 per-row consumer bound) and 4 (refined scope) + Rule A: tasks, not residue.
## 2. RELATEDNESS MAP -- THE ROOT OBJECT:
##   Define D := A_shoot - A_Efun, the derived operator-content difference (row-side sqrt(M) exchange
##   weight + Vloc-slot content; s100 G1 locus). Every instrument-level residual in the record is an
##   exposure of D:
##     E1 (static eigenvalue defect)      = <P|D|P> class          -- CLOSED s100 G1 (term-match 7e-7 rel).
##     E2 (fractional-q non-stationarity) = <dP/dq|D|P>            -- G5b. THE remaining run.
##     E3 (consumer offsets per V5 row)   = row-wise bounds of D   -- Item 3's table; same D, no new physics.
## 3. THE UNIFYING QUESTION (broad form):
##   "Given a self-consistent field generated by operator A_shoot, and an energy functional Efun whose
##   Euler-Lagrange operator differs from it by derived content D, is EVERY observed defect in the record
##   -- static eigenvalue offsets, path non-stationarity dE/dq - eps, and consumer-level operator offsets
##   -- a first-order projection of the SAME D onto the corresponding state or path derivative?"
##   If Item 1 holds 3/3 the answer is YES and the whole class closes as one theorem-shaped sentence --
##   which IS the T4 attribution clause already drafted in SCOPE-T4-S100-BROAD.
## 4. LITERATURE CLASS NAME (outward query, confirmed): "VARIATIONAL CRIMES" -- Strang 1972 (Proc. Sympos.
##   Univ. Maryland, pp. 689-710); Strang's lemmas bound the eigenvalue/solution error by a CONSISTENCY
##   TERM D_h(u,v) = a_h(u,v) - <f,v> when the discrete problem is not the EL problem of the discrete
##   functional. Term-match: our D-projection IS a consistency term in Strang's sense; our three exposures
##   are its evaluations on |P>, |dP/dq>, and the consumer functionals. Path-side corroboration: Slater
##   dE/dn = eps + convention-dependence (Baerends 1911.05651, round 2). Ledger candidates for T4:
##   Strang 1972 (class), Slater/Baerends (path exposure). The SIGN LAW remains ours (Q4 null stands).
## 5. FAULT-SPECIES CROSS-CHECK: the dominant recurring species in the carried list is SIGN error
##   (F95.3, F100.3, F93.2-species x4). Item 1's Rule B is sign-exact -- exactly where the historical
##   species strikes. Guard already in prediction: the un-symmetrised control must land OUTSIDE window.
## 6. COMBINED EXECUTION (efficiency, proposed): build D ONCE per row (58-B, 90-A, 91-A). Item 1 = the
##   three projections <dP/dq|D|P> scored P1-P3. Item 3 = the per-V5-row bound table read from the SAME
##   D receipts. One build closes both items. Item 4 then needs only the refined sentence + Rule A.
## RESIDUE COUNT UNCHANGED: ONE (G5b). But its closure now closes the CLASS, not just the item.