# RESTORE POINT 1.5

`restore-point-1.5.tar.gz` — 5.8 MB, 79 files, built with all twenty-two audits passing.

## State

| | |
|---|---|
| source | `The Method 1.4.md` |
| artefact | `The Method 1.4.pdf` — 276 pages, 33 figures |
| register | 165–559, ascending |
| mathematics | **185 objects · 177 settled · 8 unfinished · 4% open** (from 31% at the start) |
| audits | **22 of 22 pass** |
| seed of the mathematics | **14 : 185** — unmoved across every cycle |

## To restore

```
tar xzf restore-point-1.5.tar.gz -C /home/claude/work/
cd /home/claude/work && python3 "The Method 1.4 press.py"
rm -rf .zeno/audits.json && python3 The_Method_1_4_audits.py
```

Expect `BUILT … 276 pages` and `ALL TWENTY-TWO PASS`.

## The eight unfinished, and who each belongs to

**Needs Matthew — the edge list:** `W.core9` · `W.frontier` · `W.onehot` · `W.supp`'s failure count.
The violation index's exclusion classifier. Implications between axis values, of the form
`DNc = 2 → IC = 2` (the one instance the companion prints, in its withdrawals). It cuts 19,440 to
2,370. **Proof it cannot be reconstructed from counts: 2,370 = 2·3·5·79, and no product of the rungs
{4,3,3,3,5,2,2,3,3} contains 79.** Look in the computation scripts and cached datasets that
Transitions Appendix C says accompany the paper — search them for `2370`, `DNc`, or whatever printed
the §5.6 threshold table.

**Must reproduce five printed figures**, which the companion states itself: 2,370 cells in a box of
19,440 · E = 30 as 1 × 30 · core at (X=0, U=0, NEC=3) · none of 414 subsets failing · multiplicities
3, 5, 10, 30.

**Needs Matthew — one line each:** `B.hstar` (w(h) and V(h) as defined) · `T.scheme` (**how the LS
and jj brackets were derived** — register 541 shows the looseness convention does not explain them).

**Needs the species files:** `B.adm`. σ from the ASD uncertainties.

**Correctly open:** `M.C2` — half-sided modular inclusion on isolated horizons. The interval is now
named on both sides (register 558).

## Q — eleven open of fourteen

**F** located and not closed · **A · B · C · D · H · I · N · P · R** standing.
**G discharged** this session: the seed's exact computation is NP-hard (register 551).

## The method, recorded at register 553

**A source has an index, and it is rarely the one with a search box.** The ASD is indexed by
spectrum, its compilations by element, its provenance notes by isoelectronic sequence. *Seed* returns
nothing; *minimum cardinality generator* returns the theorem. Three of four searches moved by
changing the index rather than the query.

## Pinned

- CENSUS audit scope extension to ordinal self-claims
- Register 475's *fifteenth* → nine
- §30.3's possible conflation: *Γ-free* (a property of a given ordering) against *admits a Γ-free
  ordering* (a property under permutation) — register 555
