S3 = [
("read off `coords.py` rather than assumed", "read off the unbounded criterion rather than assumed"),
("Built as `termaxis.py` and tested", "Built as a term axis and tested"),
("and it is now written into `register_review.py`.", "and it is now written into the parity check."),
("spectra.py held \"101,328\" hard-coded in six places and indices.py in one", "the spectra generation held \"101,328\" hard-coded in six places and the index in one"),
("mathreg.py and status.py quote 101,328 inside HISTORICAL statements", "two of them quote 101,328 inside HISTORICAL statements"),
("ONE IS FLAGGED AND NOT FIXED: spectra_F.py asserts that even and odd ranks balance", "ONE IS FLAGGED AND NOT FIXED: the rank-balance claim asserts that even and odd ranks balance"),
("Reading indices.py at source shows", "Reading the index at source shows"),
("spectra_F.py asserted even and odd ranks balance across 101,328 cells", "The rank-balance claim asserted even and odd ranks balance across 101,328 cells"),
("was caught by phase4.py failing its own check.*", "was caught by the staging failing its own check.*"),
("*First: `phase4.py` READS", "*First: the staging READS"),
("*`store_gen.py` recovers each stored series' members from the queue2 level tables", "*The derivation recovers each stored series' members from the queue2 level tables"),
("`store_gen.py` inherits the LIMIT ASSIGNMENT from the store", "The derivation inherits the LIMIT ASSIGNMENT from the store"),
("`store_gen.py` dropped the 68 before writing", "The derivation dropped the 68 before writing"),
("*`store_gen.py` rebuilt to implement the two rules already on record", "*The derivation rebuilt to implement the two rules already on record"),
("*`store_gen.py` gains `limits`, `assign_limit`, `top_member_E`", "*The derivation gains a limit set, a limit assignment and a top-member energy"),
("Implemented in `store_gen.py` as a FIFTH status", "Implemented as a FIFTH status"),
("*`store_gen.py` closes the VALUE derivation and inherits the series list from the authored store", "*The value derivation closes and the series list is inherited from the authored store"),
("Written into `spectra.py` \u2014 the generator, not the generated file", "Written into the generation \u2014 not the generated file"),
("`series_gen.py` built from store_gen's own readers", "The series construction was built from the same readers"),
("`dclose.py` was run on the table as printed", "The closure was computed on the table as printed"),
("as generated by `mathreg.py` on 2026-08-12", "as generated on 2026-08-12"),
("Both papers pressed with build.py (no entry-form fix", "Both papers pressed (no entry-form fix"),
("from a string in build.py that has not been advanced since chat 5", "from a string that has not been advanced since chat 5"),
("so the convention was found by test (`heii.py`): three limits", "so the convention was found by test: three limits"),
("(`fig_rerender.py`, data in `fig241_data.json`", "(data in `fig241_data.json`"),
("`appf.py` now reads the two compendium files", "The press now reads the two compendium files"),
("(bracket.py, that bank's register 796)", "(that bank's register 796)"),
("Run by `run489.py` over `spectra_raw` (92 tables), limits per row with channels.py's LIM as cross-check", "Run over `spectra_raw` (92 tables), limits per row with the channel table's LIM as cross-check"),
("recomputed by `spectra_count.py`, which now sums the bracket column. `run489.py`, `ruled_bracket.py`, RULING-TOLERANCE-489.md and the run's JSON go to BUILD-10;", "recomputed by summing the bracket column. The run, its ruled bracket, its tolerance ruling and its JSON are all retained;"),
("and `series_gen.py`, the T8-era selection generator whose header", "and the series construction, the T8-era selection whose header"),
("`store_gen.py`, which series_gen.py imports, is absent from MANIFEST and delivery", "The derivation it depends on is absent from MANIFEST and delivery"),
("and store_gen.py lives in restore-point-2_13's 708 files if it ever does.", "and it lives in restore-point-2_13's 708 files if it ever does."),
("standing in for channels.py's LIM per the MANIFEST's own note that it was extracted from it. `run489.py` exactly as sealed under ruling 26", "standing in for the channel table's LIM per the MANIFEST's own note that it was extracted from it, the run exactly as sealed under ruling 26"),
("the count paragraph recomputed by `spectra_count.py`: 1,577", "the count paragraph recomputed by summing the bracket column: 1,577"),
]