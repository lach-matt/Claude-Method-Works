# B.2.13 — committed BEFORE fetching Be II.
# Targets TWO empty grid cells: test 1 second species, test 3 first instrument.

Be II is Li-like (1s2 nl), Z=4, Z_eff=3.
Appendix C delta-bar: l0 +0.2623 l1 +0.0491 l2 +0.0021 l3 +0.0001 l4 -0.0001 l5 -0.0002
-> sign change at l=3->4 (f->g), NOT at low l. Different location from He I / Li II.

B-a. Sign change at f->g CONFIRMED from measured levels: delta(nf) > 0, delta(ng) < 0.
B-b. Lattice-bracket VIOLATION at any cell whose l-neighbour set spans the f-g step.
B-c. ZERO violations at l <= 2 (s,p,d all positive and decreasing).
B-d. TEST 3, first quantitative instrument. For an ion, dT along l = 2 Z_eff^2 R d(delta)/nu^3,
     so r_l = 2 Z_eff^2 R d(delta) / (nu^3 sigma). With Z_eff=3, d(delta)~0.0002:
     r_l = 395/(nu^3 sigma).  At sigma=0.05 cm^-1 this exits (r_l<5) at nu ~ 11.7.
     PREDICT: if Be II f,g series reach n >~ 12, the f-g pair leaves the cost
     domain INSIDE its measured range - the first r_l exit observed on real data.
B-e. If the sign change is NOT present at f->g, or if violations appear at l<=2,
     the mechanism is location-dependent and does not generalise.