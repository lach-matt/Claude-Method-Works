# Committed before retrieval — B.2.13

The claim under test: connectivity of the constraint graph tracks exchange statistics.
Multiplicity axis attaches (edge) when an exclusion principle couples it; detaches
(isolated vertex) when nothing does.

C1. Lambda_9's target-spin axis ATTACHES — it carries a cap depending on another
    coordinate, not a free range. If it is free, the claim is refuted immediately.
C2. Section 6.6's "detachable factor" in Lambda is NOT a free binary axis.
    Forced: a free 2-chain sends F(-1) to 0, and Lambda has F(-1) = 2.
    So whatever detaches in 6.6 must have odd extent, or detach in a different sense.
C3. In the tower Lambda_9 to Lambda_13, the axes should SPLIT: those under an
    exclusion principle attach, those not do not. If every axis attaches, the claim
    is vacuous here; if attachment is unrelated to exclusion, it is refuted.
C4. Section 3.5's tree is CONNECTED. Lambda_sph's forest is disconnected. So the
    distinction the claim needs is already present in the book as stated.
