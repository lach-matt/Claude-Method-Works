# D1 S103 -- epsilon-linearity of the w102b F() along Dr at 6s (and 5s), row 58. Filed pre-run.
# Object: S(eps) = [F(Pm + eps*Dr) - F(Pm)]/eps, eps in {1e-3, 1e-2, 0.1, 0.5, 1.0}, exact w102b
# machinery (basis, symmetrized Imat, least-squares coords). Fork (sign/shape-exact):
#  (i) SECOND-ORDER CONFIRMED iff |S(1)/S(1e-3)| > 2 or sign differs: rot is Hessian-dominated;
#      F103.1's repair is a quadratic law. Predicted under the filed hypothesis: S(eps) ~ a + b*eps
#      with |b| >> |a|.
#  (ii) FIRST-ORDER HOLDS iff S(eps) constant within 10% across the range: the w102b rot IS
#      first-order and F103.1's mechanism is a convention/field error in w103a's Felem
#      (fault reclassifies instrument-side).