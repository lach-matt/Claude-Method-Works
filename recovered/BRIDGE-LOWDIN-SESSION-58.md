# BRIDGE — LOWDIN SESSION 58 · THE OPEN WAS RE-PROVING WHAT THE SEAL ALREADY PROVED

Open Session 59 with **`bash pack58/open58.sh`** and nothing else. It takes ~1 second.
The only number ever entered into this chain remains **c = 137.035999**.

---

## §1 · M'S RULING, EXECUTED

> Accept each handoff as sealed. Pick up where the last session ended.
> Do not re-run everything at every handoff. Condense at every seal.

s58 opened s57 in full one last time — verify ok=1140 bad=0 extra=0, gates 1..71 diff
to reference in ZERO LINES — and then rebuilt the open so that is never done again.

**NO PHYSICS MOVED. NO SEALED FILE WAS MODIFIED.** Everything below is apparatus.

## §2 · THE REDUNDANCY, MEASURED

    manifest verify        1140 files, both halves       every session
    smoke gates            13 serial, ~120 s             nlguard 94 s, gate7980 23 s
    full replay 1..71      ~180 s + poll cycles          every session

**The manifest and the replay are redundant WITH EACH OTHER.** A clean manifest proves
the bytes are identical; identical bytes plus deterministic code give identical output.
Re-running 71 gates re-proves the code — which the hash already covers.

The replay's ONE unique job is catching compiler/interpreter drift, which a hash cannot
see. That needs one probe, not seventy-one.

    OPEN:   ~300 s  ->  1.26 s
    ARCHIVE: 1657 files / 42 M / 144 root docs  ->  1033 sealed / 39 M / 32 root docs

## §3 · WHAT REPLACED IT

    LINEAGE.txt          42 sessions, one line each: N, file count, manifest ROOT HASH.
                         Replaces 41 manifests + 40 verify scripts. Any session is
                         still verifiable: rebuild the body, hash it, match root.
    pack58/condense.py   THE STANDING SEAL-TIME OPERATION. Diffs against the previous
                         seal, HALTS on undeclared retirement, folds scaffolding,
                         appends the lineage line, GENERATES the one verify.sh.
    pack58/canary.py     Environment drift in ~1 s: float/libm, kernel dlopen, numpy,
                         and the physics gate output hashed WHOLE.
    pack58/open58.sh     3 steps: seal (current only) / runtime / canary.
    pack58/seal58.sh     The expensive proofs, moved to SEAL time where they belong.
    attic/               116 superseded scaffolding files. On disk, unsealed,
                         no longer hashed every open.

## §4 · FAULTS THIS SESSION

    F58.1  MY OWN ORPHAN SURVEY REPORTED FALSE POSITIVES. A locale-unstable sort
           made derive_P*.json and three gate runners read as unsealed. Caught by
           cross-checking with cmp BEFORE reporting.        REMEDIED (LC_ALL=C)
           This is F57.3's fault class — asserting instead of reading — and it
           was caught by the discipline s57 installed.
    F58.2  THREE SEALED FILES LEFT THE ARCHIVE WITH NO RECORD:
             CENSUS-SESSION-43-POSTGATE.txt   last sealed s47
             gates_runNEXT.sh                 last sealed s52
             pack34/GATES-34-OPEN.log         last sealed s34
           None is physics data. The point is that the old process COULD lose a
           sealed file silently.                            REGISTERED + REMEDIED
           (condense.py now HALTS on any undeclared removal; can-failed by
           removing pack53/cinf.jsonl — two independent detectors fired.)
    F58.3  THE FIRST FRESH-EXTRACT TAR USED --exclude='*.tar.gz' AND DROPPED TWO
           SEALED FILES: pack49/receipts49.tar.gz, pack50/receipts50.tar.gz.
           The new open HALTED on it, as designed.          REMEDIED (exclude by
                                                            exact path in seal58.sh)
    F58.4  condense.py's first diff stripped scaffolding from one side only,
           inflating 'added' from 2 to 132.                 REMEDIED before shipping
    F58.5  canary v1 pinned ONE physics line and guessed kernel symbol names.
           It watched a third of the output it claimed to.  REMEDIED (whole-output
                                                            hash; load status only)

No fault carried open into s59.

## §5 · STANDING, AND IT REPEATS AT EVERY SEAL

1. **Open accepts the handoff.** Current seal + runtime + canary. Nothing else.
   History is NOT re-verified. If the canary reports DRIFT, and only then, run
   `pack55/gates_run55.sh 1 71`.
2. **Condense at every seal.** `python3 pack58/condense.py N --seal`. It folds that
   session's scaffolding, so the root never accumulates again.
3. **Retirement is declared.** Undeclared removal HALTS the seal (F58.2).
4. **Never sed a verify script** (F57.6). It is generated by condense.py.
5. **Surveys return counts, sets, diffs** (F57.7) — `pack57/rd.py`.
6. **Search the archive by content before declaring a gap** (s57 §6 item 5, still
   owed a ruling from M).

## §6 · KNOWN AND NOT YET FIXED

    23 shadowed basenames    Same filename in >1 pack, byte-distinct, resolved
                             SILENTLY by the flat rt/ overlay. condense.py now
                             REPORTS them; it does not yet resolve them.
    2 unsealed rt/ files     rt/TABLE-FROZEN-SPLIT-SESSION-26.txt (41 lines where
                             pack26 has 63, and frozen_split_table.py READS it),
                             rt/SESSION-LEDGER.tsv (generated state).

Neither blocks the physics. Both are reported at every seal from now on.

## §7 · ORDERED WORK LIST FOR SESSION 59 — PHYSICS RESUMES

1. `bash pack58/open58.sh`. Expect CONDENSE-CHECK CLEAN, CANARY CLEAN. ~1 s.
2. **ATTRIBUTE THE FIVE NR-1 FLAGS — the only thing Clause 3 still owes.**
   Z = 42, 43, 45, 46, 79 (Mo, Tc, Rh, Pd, Au), all s->d promotions.
   PREDICTION TO FILE BEFORE READING: all five reproduce identically at
   c = 137.035999 in restart mode, proving a format artefact and not a c-effect.
   Control is `pack53/ctrl137.jsonl`, ALREADY SEALED. **Cost: a read, not a solve.**
   If they reproduce, **Clause 3 CLOSES.**
3. **NR-4's 11 confounded steps** — [25, 30, 47, 48, 60, 61, 62, 71, 80, 103, 104].
   Same control resolves them.
4. **M's ruling owed** (s57 §6 item 5): search the archive by content before
   declaring a gap. Adopt or reject.
5. **CLAUSE 1 — the reverse chain to the many-electron Schrodinger equation.**
   Reading DONE (s53 §8 item 5). **The largest owed item in the whole solution.**
   After item 2, nothing upstream of it is open.
6. Deferred: -8.021 mHa residue at Z=59; coupled 4f2.5d SO check; promotion
   operator as an object; the 2.88 mHa residue of s56 §5.
7. **T4 (R 1701 onward) — LAST. Not opened in any working session.**

Bank restore-point-2_13 (R 1700) untouched throughout s58.