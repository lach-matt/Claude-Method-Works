# READ-ch12g — Phase R2, main volume §12.11.0.2 "The clock is an assumption, and the assumption can be removed" (chat 73)

Member The_Method_1_6-2.md (BUILD90 main, md5 4aef772b…), L2629–L2675 (47 lines; §12.11.0.3 opens at L2676), read in full against each cited section (§12.11.0.1 L2577, §7.1 L1750, §17.4 L4873, §18.4.1 L4999), the MC entry "The arrow is a property of the assumption" (MC L1292–1300; MC L2164), the Register (301, 345, 352), the rebuilt tower (tower-2.py, c0bce27a…), the segment instrument r2-ch12g.py (beside tower-2.py; the (g, G) index built from tower-2.py's Λ₈, closure tested on every pair of every set named — 94,882,200 pairs for the index itself — composition counted on all composable pairs) and r2-tools.py. Rulings applied as in READ-ch7. Census rows in range: 1060 (closed, not a defect; CENSUS-CLOSURES-ch12g.tsv).

## A. Deviations (both texts)

**12g-01 · L2652 and MC L1294 — "exactly when" fails on the restriction the section itself exhibits.** PRINTED L2652: `**A closed index of transitions has a one-way clock exactly when its destinations begin empty.**` MC L1294: `A closed index of transitions has a one-way clock exactly when its destinations begin empty; the arrow is a property of the assumption, not of the object.` SOURCE: L2657–2670 — `a temporal structure survives as a *restriction*: the maximal sub-index on which a weight never rises is X_w … which is closed`, with the occupancy row `5,165 yes`. MEASURED (r2-ch12g.py): the G = g slice (every destination begins empty) is Λ₉ — 41,682 composable pairs, 0 raising; the whole (g, G) index — 2,620,090 pairs, 826,950 raising; X_occ = {G ≤ k} — 5,165 cells, closed on all 13,341,195 pairs, **3,511 of its cells (68.0 %) with G > g, destinations that do not begin empty**, 636,250 composable pairs, **0 raising**. READING: "destinations begin empty ⇒ one-way clock" holds; "one-way clock ⇒ destinations begin empty" fails on X_occ, a closed index of transitions whose clock is one-way and whose destinations do not begin empty — the object the section's own next paragraph builds. The biconditional is an overgeneralisation (class of 9-02, 11-01, 12c-02, 12e-01); the section's conclusion (the arrow of §12.11.0.1 belongs to the assumption, not the object) stands. Subject-matter, lower. For R3: "when its destinations begin empty" (one direction), at both sites.

**12g-02 · L2672 — "§18.4.1's cap table" names a table §18.4.1 does not have.** PRINTED: `§18.4.1's cap table deciding once again what may be a coordinate, now deciding what may be a clock.` SOURCE: §18.4.1 (L4999 "The law of realised closure") has one table, L5021–5026 `open object / E / the certificate / after` (relabel, re-coordinatise, drop a coordinate — the certificates that decide what may be a coordinate); "cap table" occurs nowhere in the main but here (grep). READING: the pointer resolves to the section and the table meant is its certificate table; "cap" is the wrong word. Wording, lower.

**12g-03 · layout.** L2633–2636 (bound / origin / reading) and L2663–2670 (weight / cells / closed) are column-dumps; L2635 and L2649–2650 are four-space lines that render as code. Production.

## B. Verified (all MEASURED unless marked; r2-ch12g.py)

- L2633–2636 the two bounds on g and their origins are §7.1's table L1756–1759 (`g ≤ 2(2f + 1) Pauli exclusion`; `g≤q counting — cannot place more than were removed`); L2641 `seven constraints, four origins and nothing else` is L1762 verbatim; `register 301 found it short one condition` — R L1129 `short an eighth condition … k ≥ 1`.
- L2638 `They coincide exactly when the target subshell begins empty`: g = G iff the prior occupancy G − g is 0 — literal.
- L2644–2647 the (g, G) index — every Λ₈ cell with G ∈ [g, 2(2f + 1)] and 2S′ ∈ [0, G], g ≤ q inherited: **13,775 cells** (with 2S′ ≤ g instead, 6,000 — the print's 2S′ ≤ G is the construction that reproduces); its G = g slice is Λ₉, 1,654; **closed** — every one of the 94,882,200 pairs has join and meet in the set. The four bounds are single-parent monotone (INFERRED from their form). §17.4 (L4873 "A worked extension, and what it costs") resolves to the sum-indexing move; MC L2164 carries the same figures.
- L2649–2650 composable pairs, matching (e, f, G, 2S′)_a = (n, ℓ, k, 2S)_b: **2,620,090**; pairs whose first step raises occupancy, G_a > k_a: **826,950 = 31.6 %** — the printed figures. The share depends on which step of the pair is read: second step (G_b > k_b) 1,621,290 = 61.9 %; the composite (G_b > k_a) 1,646,760 = 62.9 %; per cell, 8,610 of 13,775 = 62.5 % have G > k, and 1,410 of the 6,090 cells with a successor = 23.2 %. The print is the first-step count over pairs; Register 352's "31.6% of composable steps" reads "steps" as pairs. Matching on placed instead ((e, f, g, 2S′)_a) gives 1,176,779 pairs — not the print.
- L2657–2670 the seven weight rows on the (g, G) index: shell e ≤ n **9,407** closed (44,250,528 pairs); subshell f ≤ ℓ **9,845** closed (48,466,935); occupancy G ≤ k **5,165** closed (13,341,195); spin 2S′ ≤ 2S **7,160** closed (25,636,380); e + G ≤ n + k: 4,897 cells, 579,460 joins and 171,552 meets leave the set — "breaks both sides"; max(e, G) ≤ max(n, k): 5,151 cells, every join in, 578,416 meets out — "breaks meets"; min(e, G) ≤ min(n, k): 8,623 cells, 2,681,376 joins out, every meet in — "breaks joins". Occupancy the most restrictive of the four (L2673–2674). "One such arrow per coordinate and no others": the four coordinate arrows are closed and the three named combinations are not; "no others" is measured on those three.
- L2653–2654 §18.4.1 (L4999) resolves; `a zero is worth the size of the box it was computed in` is L5075's sentence within §18.4.1 (also L865, L1387, L2716).
- L2674 Register 352 (R L1287) resolves and carries the section (`31.6% of composable steps raise occupancy … one arrow per coordinate and no others`); MC L1292–1300 carries it entire, its scope line naming the caps (n, e, ℓ, k, f) = (3, 3, 1, 3, 1) and the ℛ(X) = X sweep.
- Census row 1060 (C9 "never", L2659 `the maximal sub-index on which a weight never rises`): the definition of X_w — literal, not a defect.

## C. Incidental

- 2,620,090 and 31.6 % are cap-dependent figures at (3, 3, 1, 3, 1) (MC L1296 says so); the section claims no variation for them, and none was run.
- The failure of a sum, a max and a min of coordinates under coordinatewise ∨ / ∧ is general (INFERRED) and measured here in the sides the text names.
- r2-ch12g.py's `closed()` tests any coordinate-defined subset on every pair with a dense box lookup (94.9 M pairs in well under the timeout) — reusable for the closure claims of §12.11.0.3–0.4.
