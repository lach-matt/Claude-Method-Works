# HANDOFF-74 — The Method 1.6 — chat 121 → chat 122

- Written from **chat 121** for **chat 122**. Live files: **BUILD90 main** (unchanged since chat 62)
  and **BUILD150 compendia** (= BUILD149 + W-160 + DEF-121 + six new members). Register **1 to
  1792** (no Register entry since the chat-67 hold). W-160 IS seated; chat 122 seats nothing at open
  and writes W-161 at close.
- **Read RULINGS-R2.md at open, last block first.** The chat-95 block still governs and is still the
  last: **a finding is not a question.** A deviation in the mathematics or in the prose is recorded
  and **flagged for repair**, never put to M. **Prints & Proofs is read before any question is
  asked.** The chat-81 cadence is unchanged: read, census in two kinds, exactly two instrument
  batches, never split a section read.
- **§32.1 is CLOSED.** Chat 121 cut **L8889–L9029, 141 lines** — §32.1.2, §32.1.3, §32.1.4 and
  §32.1.4.1, the movement whole, ending where §32.2 begins. **Main volume now 76.1 % read
  (L9029 of 11,855).**
- **MEASURED main-volume heading lines, to be re-taken by your own scan:** §32.2 **L9030**, §32.3
  **L9038**, §32.4 **L9059**, §32.4.1 **L9085**, §32.4.2 **L9120**, §32.5 **L9157**, §32.5.1
  **L9168**, §32.5.2 **L9185**, §32.5.3 **L9192**, §32.6 **L9205**, §32.6.1 **L9267**, §32.7
  **L9307**, `# PART VII` **L9393**, `## 33.` body **L9399**. **Resolve every heading to its BODY
  occurrence** — `## 30.`, `## 31.`, `## 32.`, `## 33.` and `## References` each have two
  occurrences (contents at L150–L173, body at L8316, L8609, L8790, L9399, **L11503**); taking the
  contents hit reports every attribution unbibliographed. **16 `PART` labels: 8 contents
  (L110–L155) and 8 body (L175, L232, L1488, L3677, L5549, L5937, L7364, L9393).**
- **The natural next unit is §32.2 through §32.4 (L9030–L9084, 55 lines)** — three short sections
  that form one movement (self-reference, self-defence, the recursion) — **or §32.2 through §32.4.2
  (L9030–L9156, 127 lines)** if the recursion is read with both its subsections, which is the cut
  that closes the movement. Cut it yourself, re-measuring first, and say which you chose.
- **§32.2 is already partly read and is a live thread.** Chat 121 read it in full (body_range =
  section_span = (9030, 9038), COINCIDE): it prints **S1 alphabet, S2 order, S3 bounds**, and
  **S3 as printed is the claim §32.1.2 tested and reported failing in its strong form (18 of 68,
  26 %)**. Read S3 against 16o-01: the 4 % baseline there is computed over **twenty-five chapters**
  where the volume carries **36**, so the true multiple is 9.53×, not the printed 6.5×. Also
  **16m-08's reverse members L11555 and L11799 both cite §32.2 for a listing of unobtained works it
  does not carry** — test that again from §32.2's side now that the section is in the unit.
- **§32.6 L9205 remains the older open thread:** it prints *The three conditions are tested here*,
  and §31.3.4 L8755 calls its own test *the fourth falsification test* with no printed enumeration
  containing a fourth (16j-05). §32.6 L9220–L9226 records one FAILED check and says the chapter was
  written as 31 while the contents listed it as 21. Chat 120 measured §32.6's body_range (9205,9267)
  against section_span (9205,9307): they **DIFFER**.
- **DO NOT RE-DERIVE these, measured in chat 121 inside its own unit:** the enlargement table's five
  rows **976 / 8,853 / 89,438 / 267,858 / 499,246**, all exact, with **|ℛ(Λ)| = |Λ| and E = 0
  measured at the first three** and the last two bounded by a stated 4,000,000-cell ambient-box
  budget (a budget bound, **not** a negative); **18/68 = 26 % exact under all three conventions** and
  **26/4 = 6.5**; **5 → 25 fivefold**; **4 + 1 + 33 + 17 = 55** and **4 + 4 + 23 + 17 = 48**, both
  exact; **all twenty registers cited in the unit exist and are on point** (236, 237, 203, 207, 213,
  215, 218, 220, 221, 227, 305, 310, 318, 320, 321, 285, 294, 278, 332, 373); **§18.6's heading is
  literally *E(X) is the prediction budget***; **§12.11.3.1 carries the law/extent division as a
  marked table column**; **§17.2 read whole is a predicate on a supplied h** (100 % over 424 tests,
  Theorem 17.1); **PART I = THE PROCEDURE, PART VI = THE RECORD AND THE REACH, PARTS II and III =
  THE LATTICE and THE LAW**; the Register at **1,628 bare + 7 grouped = 1,660 distinct entry
  numbers**; and **15 word-form sites of *one thousand six hundred and thirty-five*** with the digit
  form at **L7373 only**.
- **A probe that scores zero on its own volume is broken, and the two-line join must normalise
  whitespace** — body lines carry a leading space, so `line + ' ' + next` produces a double space and
  a witness spanning the wrap misses. Chat 121's faults 3 and 4; both had scored exact Prints &
  Proofs witnesses absent.
- **Match a grouped Register heading on commas as well as dashes.** The Register's seven grouped
  headings are comma-lists (`### 207, 209, 213, 219, 220, 221, 231`); a dash-range pattern sees
  **none** of them. Chat 121's fault 1.
- **State what a sweep covered, and print the section rather than probing it** when a section is
  short: chat 121's §7.4 probe returned nothing and the repair was to print all 7 lines. Fault 2.
- **Report sentence-INITIAL capitalised tokens too.** An attribution can open a sentence, and a
  lookbehind that skips sentence-initial words hides it. Fault 5.
- **One global Prints & Proofs offset holds across THIS unit but must still be re-anchored per
  witness:** chat 121 measured **−94** at ten witnesses spanning L8889–L9013 (one reports −95 by join
  alignment), where chat 120 measured −88 and −94 inside a single 99-line unit. **Anchor every PP
  witness on its own text.**
- **`body_range` and `section_span` DIFFER wherever a section has subsections.** In chat 121's
  pointer set **§18.6, §17, §22.4 and §32.1.4 DIFFER**; **§32.2, §12.11.3.1, §18.4.1 and §7.4
  COINCIDE**. Resolve under both, always, and **when they coincide say so**.
- **Census rows: measure them yourself** from DEFECT-CENSUS.tsv keyed on the column named `member`,
  whose values are `all`, `ioi`, `main`, `mc`, `pc`, `reg`, `sc` — **there is no per-filename value**.
  The columns are `id class member line item detail`. **Sweep classes `main` AND `all`**. Chat 121's
  range held **two**, ids 1189–1190, both C9-OVERGENERALISATION-WORD on *never*, both disposed as
  regex artefacts because the flagged token is the section's own claim.
- **A pointer-site regex must be `§N(?!\\d)(?!\\.\\d)`, and a lettered pointer needs its own pattern** —
  `§([A-Z]\\.\\d+(?:\\.\\d+)*)`. §F.4.1 (L11330) and §F.4.3 (L11343) resolve only through
  `lettered_heading`.
- **A digit-bounded numeral sweep must be `(?<![\\d.,])N(?!\\d)(?!,\\d)(?!\\.\\d)`.** **Report numeral
  SITES, not counts.** A figure spelled in words (*fifty-five*, *forty-two*) is invisible to it —
  sweep the word form too.
- **A heading-RANK scan cannot end a span.** **`heading_line` requires a trailing space after the
  number**, so the Register's bare `### 96` headings return None — locate Register entries with
  `^#{1,4}\\s*N\\s*$` **and a grouped match**; it also returns None for lettered appendix headings.
- **`withdraw` is not the stem — `withdrew` does not contain it.** Use **`withdr`**. Sweep `retract`
  and `supersed` alongside it (measured 1 and 97 in the Register).
- **The ℓ coordinate is tuple index 1, not 2** — index 2 is n′. **Parts carry no markdown heading at
< truncated lines 87-508 >
first. Run HANDOFF-74's §0 gate in full and in order — fetch both bundles by title, bootstrap
(decode, md5, extract, expect 552 files), fetch the Prints & Proofs original 'The Method 1.6.md'
(738,550 B, md5 49900cf41f818ab789bb90fc596ac977) to /home/claude/PP_The_Method_1_6.md because
thirteen banked instruments read it, then gate.py census, run --core, manifest, run r2-ch16m
r2-ch16n, cert 122; any FAIL stops the chat with a report. Read RULINGS-R2.md last block first: the
chat-95 block governs and it says a finding is not a question — deviations in the mathematics and in
the prose are recorded and flagged for repair, never put to M, and Prints & Proofs is read before any
question is asked. Do not ask M to rule on a defect. Read DEFERRED.md; chat 121's block is the last
of forty-nine chat blocks (fifty ## headings). The standing block's Phase 0–4 Löwdin/three-body plan
is executed carried state; discard it per Ruling 41 — its discard is W-118. Line numbers are MEMBER
line numbers and are never carried between chats, and neither is any count or any heading list.
§32.1 is CLOSED: chat 121 closed L8889–L9029. §32.2 opens at L9030, §32.3 at L9038, §32.4 at L9059,
§32.4.1 at L9085, §32.4.2 at L9120, §32.5 at L9157. Re-measure by heading scan, resolving each
heading to its BODY occurrence — '## 30.', '## 31.', '## 32.', '## 33.' and '## References' each have
two occurrences and taking the contents one reports every attribution unbibliographed. The proposed
unit is L9030–L9156 (§32.2 through §32.4.2, the recursion read with both its subsections, 127 lines)
or L9030–L9084 (55 lines) at the low end of the measured rate. Cut it yourself and say which you
chose, preferring the cut that closes a movement. Never split a section read across chats. §32.2 is a
live thread from both sides: chat 121 read it whole (S1 alphabet, S2 order, S3 bounds) and found S3
carrying the claim §32.1.2 tested at 26 %, while 16m-08 has L11555 and L11799 both citing §32.2 for a
listing of unobtained works it does not carry — test it from the cited side now. §32.6 at L9205 is
the older open thread: it prints 'The three conditions are tested here' while §31.3.4 L8755 names
'the fourth falsification test', and §32.6 L9220 records a FAILED check. Count every count word
against its own body, its row labels, its numeral span and its own body's status markers. Read every
cited criterion IN FULL at its target before accepting OR rejecting the conclusion drawn from it:
chat 121 found §22.4 cited for a closing result about language that §22.4 does not carry — 'no
language for' has exactly one site in the whole volume, the citing line itself — and in the same unit
found §17.2 read whole DOES carry what L8991 attributes to it, so the read clears as often as it
convicts. Re-measure every figure a section computes about the BOOK against the book as it now
stands: chat 121 found an S3 baseline computed over twenty-five chapters where the volume carries 36,
which makes the true multiple 9.53× and the printed 6.5× an understatement. State a budget rather
than a negative when a computation is too large — chat 121 bounded two rows of the enlargement table
by a stated 4,000,000-cell ambient box. Before scoring a percentage wrong, run all three conventions
and name the one that reproduces; and check whether a row of rounded percentages can reach its
printed total at all — three partition percentages must total 99, 100 or 101, and chat 121 found two
rows totalling 98. Match a grouped Register heading on commas as well as dashes: the seven grouped
headings are comma-lists and a dash-range pattern sees none of them. A probe that scores zero on its
own volume is broken, not a finding, and the two-line join must normalise whitespace because body
lines carry a leading space. Report sentence-INITIAL capitalised tokens as well as mid-sentence ones,
or an attribution opening a sentence is hidden. Where a section is short, print it rather than probe
it. Check that a stem contains the word's other tenses: 'withdraw' does not match 'withdrew'. L8_at
returns 8-tuples whose index 1 is ℓ, not index 2, and parts carry no markdown heading at all — 16
PART labels, 8 contents and 8 body. Measure the census rows in range yourself from DEFECT-CENSUS.tsv
keyed on the column named member, whose values are all, ioi, main, mc, pc, reg and sc — there is no
per-filename value; sweep classes main AND all; chat 121's range held two, both C9 artefacts on
'never'. Do not re-derive chat 121's findings: the enlargement table's five rows 976 / 8,853 /
89,438 / 267,858 / 499,246, all exact, with E = 0 and |ℛ(Λ)| = |Λ| at the first three; 18/68 = 26 %
under all three conventions and 26/4 = 6.5; 5 → 25 fivefold; 4+1+33+17 = 55 and 4+4+23+17 = 48; all
twenty cited registers present and on point; §18.6's heading being literally 'E(X) is the prediction
budget'; §12.11.3.1's law/extent table column; §17.2 as a predicate on a supplied h; PART I = THE
PROCEDURE, PART VI = THE RECORD AND THE REACH, PARTS II and III = THE LATTICE and THE LAW; the
Register at 1,628 bare + 7 grouped = 1,660 distinct entry numbers; and fifteen word-form sites of
'one thousand six hundred and thirty-five' with the digit form at L7373 only. Then continue Phase R2
under the chat-81 cadence: read the unit in full, census its claims into computable and prose, then
run exactly two instrument batches, r2-ch16o computable and r2-ch16p prose, importing heading_line,
section_span, has_token, enclosing, Rset, L8_at and is_tree from r2lib — copy nothing, pass them the
LINE LIST and not the member text, read the six volume MEMBERS never a BUILDnnn bundle path, and note
that tower-2.py's L8 is a FUNCTION, so call it. A digit-bounded numeral sweep must be
(?<![\\d.,])N(?!\\d)(?!,\\d)(?!\\.\\d), numeral SITES must be reported rather than counts, and a
figure spelled in words is invisible to it — sweep the word form too. A symbol is tested raw and
never transliterated. has_token is letter-bounded on BOTH sides, so use a left-bounded matcher for a
stem, and test a Ruling 46 token case-sensitively and word-bounded or BUILD matches rebuilds. A
colon-terminated lead-in is not a list item. Anchor every Prints & Proofs witness on its own text —
chat 121 measured a uniform −94 across ten witnesses where chat 120 measured −88 and −94 inside one
99-line unit. body_range is the resolver for a section body and section_span for a chapter; resolve
every pointer under BOTH before recording an absence, say so when they coincide, and locate where the
claim does live. A pointer-site regex must be §N(?!\\d)(?!\\.\\d), and a lettered pointer needs its
own pattern, §([A-Z]\\.\\d+(?:\\.\\d+)*). A heading-RANK scan can never end a span. A token probe is
not a reading. A claim test must exclude the heading line. Never canonicalise set orbits with min over
frozensets — it is a subset partial order. Sweep: every attribution against the BODY occurrence of
## References and against R.7, and the reverse direction too; every register citation against its
entry's headline, grouped-aware and existence first; every section citing another for a figure the
cited section later withdrew. Figure references are a production layer: the volume has 33 and PP has
none, no .png is a member, and an unresolvable image path is NOT a text defect. The book's present is
2026. Expect the instrument to be wrong before the book — that fired twice in chat 121, twice in 120,
twice in 119, three times in 118. Name the pair convention before reporting any pair count, the
monomial order before any Groebner basis size, and the closure convention before any E(X). Never
round with Python's round(); use Decimal.quantize and name the convention. Give every negative claim
its own witness and state what a sweep covered. Record the passes as well as the failures, or a class
will look worse than the book is. Index each volume once rather than rescanning it per phrase. Close
the section read before the next opens. At close: bank both goldens with gate.py bank, write W-161
ending with a blank line, build BUILD151 with close.py (reverse must recover efe4fc5c…), write
HANDOFF-75 BEFORE the final verification, and begin the close with at least eight tool calls left.
No corrections, no Register entries, no TASK 1 until the review closes. Handoff at 90–95% of context
or on a closed section read — never earlier, never mid-section. Timeout on every call. Delete-only
calls for pycache, never chained to any other command. Never copy over an existing file."
