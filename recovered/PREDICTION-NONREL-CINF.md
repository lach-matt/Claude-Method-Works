# PREDICTION — CLAUSE 3, THE NON-RELATIVISTIC LIMIT
# Filed $TS. BEFORE any c=1e6 row is computed or read. R 1449.
# Instrument: c is a DEFAULT ARGUMENT on eigen_sr / numerov_wf_sr / scf_occ_sr.
# c = 1e6 is this project's ESTABLISHED non-relativistic limit, already used as a build
# gate ("SR at c=1e6 regenerates the T0b baseline d to 1e-4"). Not a new instrument.
# WHY THIS MATTERS: Löwdin clause 3 specifies the NON-RELATIVISTIC many-electron
# Schrödinger equation. The sealed walk uses a scalar-relativistic field. If the ordering
# clause survives c -> infinity, the relativistic layer is a CORRECTION the result
# survives, not a premise it needs, and clause 3's exposure closes.

NR-1  THE ORDERING CLAUSE SURVIVES. Zero ordering failures over Z = 2..108 at c = 1e6.
      This is the load-bearing clause. If it fails, the derivation is relativistic and
      clause 3 does NOT close -- and that outcome would be reported, not reinterpreted.

NR-2  THE THREE TIE-BREAK FAILURES PERSIST: {57, 89, 90} at c = 1e6, unchanged.
      They are collapse-domain effects, not relativistic artefacts. If instead they
      VANISH at c = 1e6, then Deliverable 3's collapse condition is entangled with the
      relativistic layer and §3 of Deliverable 1 must be reopened.

NR-3  DEPTHS MOVE, ORDER DOES NOT. |D(c=1e6) - D(c=137.035999)| is monotone-increasing
      in Z in the aggregate: below 1 mHa for Z < 30, largest for Z > 80.

NR-4  IF ANY STEP CHANGES ITS ENTRANT, IT IS AT Z >= 72, in the 5d/6s or 6d/5f/7s
      competitions where the SR shift is largest and the sealed margins are smallest.
      No change is predicted below Z = 72.

NR-5  THE g CHANNELS ARE UNMOVED. They sit at -1/(2n^2) at c = 137.035999; predicted
      identical to within 1e-5 at c = 1e6, because a channel not in the field cannot
      carry a relativistic correction. This is a consistency check on NR-3's mechanism.

NR-6  THE cfg AND ok SCORES ARE LARGELY UNCHANGED: cfg within +/-3 of 73/107 and ok
      within +/-3 of 96/107. The promotions of §5(a) are not relativistic in origin.

SCORING: a clause HOLDS only if the c=1e6 chain shows it without reinterpretation.
NR-1 is DECISIVE. A clause reworded to pass is FALSIFIED and recorded as such.