# HANDOFF-78 — The Method 1.6 — chat 125 → chat 126

- Written from **chat 125** for **chat 126**. Live files: **BUILD90 main** (unchanged since chat 62)
  and **BUILD154 compendia** (= BUILD153 + W-164 + DEF-125 + six new members). Register **1 to
  1792** (no Register entry since the chat-67 hold). W-164 IS seated; chat 126 seats nothing at open
  and writes W-165 at close.
- **Read RULINGS-R2.md at open, last block first.** The chat-95 block still governs and is still the
  last: **a finding is not a question.** A deviation in the mathematics or in the prose is recorded
  and **flagged for repair**, never put to M. **Prints & Proofs is read before any question is
  asked.** The chat-81 cadence is unchanged: read, census in two kinds, exactly two instrument
  batches, never split a section read.
- **Chapter 33 is CLOSED and Part VII is open.** Chat 125 cut **L9393–L9493, 101 lines** — the
  `# PART VII` divider, the chapter-33 head and §33.1–§33.5 entire — ending where `## 34.` opens.
  **Main volume now 80.1 % read (L9493 of 11,855).**
- **MEASURED main-volume heading lines, to be re-taken by your own scan:** `## 34.` body **L9494**,
  §34.1 **L9500**, and Chapter 34 runs to `## 35.` body at **L9716**. That is **222 lines**, well
  above the measured 74–150 band, so **Chapter 34 must be cut into two or three units** — take the
  §34.N heading lines yourself and choose a cut that closes a movement. **Resolve every heading to
  its BODY occurrence**: `## 34.` and `## 35.` have contents hits at **L157** and **L158**; `## 30.`,
  `## 31.`, `## 32.`, `## 33.` and `## References` likewise (body at L8316, L8609, L8790, L9399,
  **L11503**). Taking a contents hit reports every attribution unbibliographed. **Chapters 20 and 21
  are the exception:** their contents entries are plain body lines at L138–L139.
- **Chapter 34 is the Löwdin challenge, and its neighbourhood is already partly measured** — do not
  re-derive: the epigraph at **L9496–L9498** (chat 125's 16v-06 twin), *the domain protocol blocked
  four fits* at **L9685**, §35.1's repaired *Chapter 33 said* at **L9729–L9730**, and 1969 sites at
  L9600, L9722, L9832, L9834, L9868, L9874. **Chapter 34 is where the domain protocol's four fits are
  claimed; test that count against register 1336's own replay, which records three BLOCKED and two
  PASS.**
- **`section_span` CANNOT bound a section that ends a Part** (chat 124's C1). Chat 125's counter-case:
  for **§33.1–§33.5 the two resolvers COINCIDE at five of five**, because no §33 section ends a Part.
  **Resolve under BOTH, always; say so when they coincide; bound the unit by `body_range`.**
- **DO NOT RE-DERIVE these, measured in chat 125 inside its own unit:** `L8_at((3,3,1,3,1))` = **976**
  cells exact against L9423; the count word *Six* exact against **6 DATA rows** at L9414–L9421;
  **Chapter 12 holds exactly fifty headings numbered 12.x inside its body span L2304–L3507** (the
  fifty-first, §12.11.8, sits at L3543 outside it — the misplaced heading closed as a finding in chat
  80); L9406's Chapter 20 citation **exact on both halves** (§20.1 heading L5556, claim L5562);
  register **1300** carrying the singleton criterion, the n\* result and the Λ_ryd result verbatim;
  Λ_var and Λ_ryd both existing (ioi L1596, L1614); **Ruling 46 zero sites**; **first-person zero
  sites**; **PP 74 for 74 at −94**; **1 of 46 long lines recurring**, and that one is 16v-06's
  epigraph; **zero unmarked sub-headings**; **one census row, disposed as a regex artefact**.
- **PP prints the five §33 section headings UNNUMBERED** (`### Where it came from` at P9310) where
  the volume numbers them (`### 33.1 Where it came from`). **Section numbering is a production
  layer.** A whole-line PP comparison reports five phantom absences; compare the heading's **title**.
- **CONVENTIONS worth keeping.** A roster or list that **wraps across two lines** must be read on the
  **whitespace-normalised join** — chat 125's §20.2 parse returned three names of six from one line.
  An **unmarked-sub-heading test must require a blank line above**, or it catches the wrap tails of
  italic sentences. The volume heads appendices **`## Appendix X — …`, twice**, so `lettered_heading`
  returns None for Appendix B and E — use a **body-occurrence appendix resolver**; `heading_line` is
  **numeric-only**, so §E.1.4 resolves to None though the volume heads it at **L10986** — a lettered
  pointer needs **`§([A-Z]\.\d+(?:\.\d+)*)`**. A pointer-site regex must be **`§N(?!\d)(?!\.\d)`**.
  A caption count is not a caption test. **`\b(I|my|we|our)\b` matches the Roman numeral in *He I***
  — guard it. **A literal species string is not a species test.** **A count word over a table counts
  DATA rows.**
- **The book numbers its principles P1–P23 with P10, P12 and P18 unassigned — twenty in all (L233).**
  Resolve any principle citation to the **P-form**. **Chat 125 adds:** the language-bounds claim is
  assigned to **P2** at L366 and headed **P20** at L396, while §20.1 L5559 attributes it to P20 —
  out of unit, docket 33's shape at a new object.
- **Census rows: measure them yourself** from DEFECT-CENSUS.tsv keyed on the column named `member`,
  whose values are `all`, `ioi`, `main`, `mc`, `pc`, `reg`, `sc` — **there is no per-filename
  value**. Columns are `id class member line item detail`. **Sweep classes `main` AND `all`.** Chat
  125's range held **one**, disposed as an artefact.
- **Line numbering.** Main-volume lines are numbered on the **member** (`The_Method_1_6-2.md`,
  1-based, 11,855 lines); the bundle numbers each line one higher because of its `<<<FILE:` header.
- **The book's present is 2026.** **Figure references are a production layer:** 33 inline
  `![Figure …]` references, none in PP, no `.png` is a member, and an unresolvable image path is
  **NOT** a text defect.
- **Carried state to discard (Ruling 41):** the standing prompt block's "Phase 0–4"
  Löwdin/three-body plan is executed carried state; do not re-open it or put it to M. Its discard is
  **W-118 (chat 81)**. Project knowledge holds BUILD12/BUILD53 only — list it, never read those
  bundles. Retired handoffs are gone from Drive; never fetch or cite one. **Never add HANDOFF-NN.md
  as a member.**
- **Known and pre-existing:** the compendia bundle carries **48 legacy `HANDOFF-NN.md` members**,
  identical in BUILD122–154. They extract into `members/` harmlessly, but a handoff must never be
  written into `members/` or passed to `--members`.
- **Findings carried to R3:** the READ-chNN.md members (READ-ch16v is chat 125's) and W-101…W-164 in
  WORKING-REGISTER.md; nothing is restated here. **Deferred cross-chapter items:** `DEFERRED.md`
  (**54 `##` headings — 53 chat blocks plus the verbatim HANDOFF-25 block; chat 125's is the last**)
  governs; do not re-derive.
- **One open scope question, put to M by chat 113 and not yet answered** (a scope choice, not a
  finding, so properly M's): should R2's remaining scope stay a full source-order read of all six
  volumes, or should the five compendia be read against what the transversal sweeps have already
  covered rather than line by line? Measured basis: main volume now **80.1 % read at L9493**, the
  other five volumes 0 % in source order but swept transversally by every batch since chat ~90;
  measured rate ~74–150 main-volume lines per chat. **Do not re-ask it unprompted; carry it.**

## §0 Gate (each step its own tool call under `timeout 280`; any FAIL stops the chat with a report)

1. `ls -la /mnt/user-data/uploads /mnt/user-data/outputs /home/claude`; `recent_chats` confirms the
< truncated lines 89-463 >
- **Keep BUILD124** for now — it is the only input under which chat 96's `r2-ch14l` and `r2-ch14m`
  reproduce, and §23.9.1's eight exact V values live in `r2-ch14l.out`. Read those values out of the
  **member** rather than re-running the instrument.
- **Keep:** BUILD90 main (unchanged since chat 62), the **Prints & Proofs** folder
  (`1vYctzLppUZ2vFPJDCZ6wvkgSdj3v_f6n`) — the authentication baseline under Ruling 56 and required
  by the gate itself, now read by **twenty-one** banked instruments (MEASURED) — the certificates,
  OWED-REGISTER-EXPANSIONS.md, OWED-EXPANSIONS-2.md, covers8.json, factor.py.

## Prompt for chat 126

"Chat 126. READ EVERYTHING BEFORE YOU DO ANYTHING. Live files: BUILD90 main (1,983,081 B, md5
49065309b0c4fe8e055f693aed295cca) and BUILD154 compendia (7,295,512 B, md5
dad6b9fb6985c4fe398cf5b030eb1b0c, 96,268 lines, 574 members). List uploads, outputs and /home/claude
first. Run HANDOFF-78's §0 gate in full and in order — fetch both bundles by title, bootstrap
(decode, md5, extract, expect 576 files), fetch the Prints & Proofs original 'The Method 1.6.md'
(738,550 B, md5 49900cf41f818ab789bb90fc596ac977) to /home/claude/PP_The_Method_1_6.md because
twenty-one banked instruments read it, then gate.py census, run --core, manifest, run r2-ch16u
r2-ch16v, cert 126; any FAIL stops the chat with a report. Read RULINGS-R2.md last block first: the
chat-95 block governs and it says a finding is not a question — deviations in the mathematics and in
the prose are recorded and flagged for repair, never put to M, and Prints & Proofs is read before any
question is asked. Do not ask M to rule on a defect. Read DEFERRED.md; chat 125's block is the last
of fifty-three chat blocks (fifty-four ## headings). The standing block's Phase 0–4 Löwdin/three-body
plan is executed carried state; discard it per Ruling 41 — its discard is W-118. Line numbers are
MEMBER line numbers and are never carried between chats, and neither is any count or any heading
list. Chapter 33 is CLOSED and Part VII is open: chat 125 closed L9393–L9493, the Part VII divider
plus §33.1–§33.5 entire. '## 34.' body opens at L9494, §34.1 at L9500, and Chapter 34 runs to '## 35.'
body at L9716 — 222 lines, ABOVE the measured 74–150 band, so cut it into two or three units. Take
the §34.N heading lines by your own scan, resolving each heading to its BODY occurrence ('## 34.' and
'## 35.' have contents hits at L157 and L158), say which cut you chose, prefer the cut that closes a
movement, and never split a section read. Resolve every pointer under BOTH body_range and
section_span, say so when they coincide — chat 125's five sections coincided at five of five, the
counter-case to chat 124's §32.7, which differed only because it ended a Part — and bound the unit by
body_range. Read every cited criterion IN FULL at its target before accepting OR rejecting the
conclusion: that is how chat 125 found three of its seven deviations, including L9449 citing Chapter
27 for a criterion Chapter 27 does not contain, which register 1755 had already diagnosed and
repaired at §35.1. Grep the Register for a later or exact statement before recording any figure as
unreproducible, and check whether a later entry SUPERSEDED the figure the text still prints — register
1327's eleven closed indexes became register 1347's twelve, and 'nine of eleven' still stands
unmarked at three sites. Read every roster or list on the whitespace-normalised JOIN of its lines, not
one line — chat 125's §20.2 parse returned three names of six. A heading that fails a verbatim Prints
& Proofs match at its offset is not absent from PP: the volume numbers its section headings and PP
does not, so compare the heading's TITLE. An unmarked-sub-heading test must require a blank line
above, or it catches the wrap tails of italic sentences. Count every count word against its own DATA
rows, its numeral span, its own body's status markers AND the Register entry that restates it. Name
the convention before scoring any figure — a combination count needs its combination convention, and
C(6,2) = 15. Never round with Python's round(); use Decimal.quantize and name the convention. A
numeral sweep must match the printed thousands separator and the word form too. The volume heads
appendices '## Appendix X — …' twice, so lettered_heading returns None for Appendix B and E — use a
body-occurrence resolver; heading_line is numeric-only, so §E.1.4 resolves to None though the volume
heads it at L10986 — a lettered pointer needs §([A-Z]\.\d+(?:\.\d+)*). A pointer-site regex must be
§N(?!\d)(?!\.\d). A caption count is not a caption test. \b(I|my|we|our)\b matches the Roman numeral
in 'He I'. A literal species string is not a species test. Measure the census rows in range yourself
from DEFECT-CENSUS.tsv keyed on the column named member, whose values are all, ioi, main, mc, pc, reg
and sc; sweep classes main AND all; chat 125's range held one, disposed as a regex artefact on a live
claim. Do not re-derive chat 125's findings: 976 exact from the rebuilt lattice, the count word Six
exact against six DATA rows, Chapter 12's fifty sections exact inside its body span, L9406's Chapter
20 citation exact on both halves, register 1300 carrying all three §33.4 claims verbatim, the printed
§33 structure matching register 1359, Löwdin's 1969 date right and bibliographed at L11809, PP 74 for
74 at −94 with the five headings present unnumbered, 1 of 46 long lines recurring, and zero Ruling 46
sites, zero first-person sites and zero unmarked sub-headings. Then continue Phase R2 under the
chat-81 cadence: read the unit in full, census its claims into computable and prose, then run exactly
two instrument batches, r2-ch16w computable and r2-ch16x prose, importing heading_line, section_span,
has_token, enclosing, Rset, L8_at and is_tree from r2lib — copy nothing, pass them the LINE LIST and
not the member text, read the six volume MEMBERS never a BUILDnnn bundle path, and note that
tower-2.py's L8 is a FUNCTION, so call it, and that L8_at takes (n_max, e_max, l_max, k_max, f_max) in
that order. r2lib.factor_q is a Λ₉ function and cannot be called on 8-tuples. Report numeral SITES
rather than counts. A symbol is tested raw and never transliterated. has_token is letter-bounded on
BOTH sides, so use a left-bounded matcher for a stem, and test a Ruling 46 token case-sensitively and
word-bounded. A colon-terminated lead-in is not a list item. Anchor every Prints & Proofs witness on
its own text. A heading-RANK scan can never end a span. A token probe is not a reading; where a
section is short, print it rather than probe it. An identity entailed by another is not an independent
test. A Register entry restating a count is not independent corroboration of it. Sweep: every
attribution against the BODY occurrence of ## References and against R.7, and the reverse direction
too; every register citation against its entry's headline, grouped-aware and existence first; every
section citing another for a figure the cited section later withdrew. Chapter 34 claims 'the domain
protocol blocked four fits' at L9685 — test that against register 1336's own replay, which records
three BLOCKED and two PASS. Figure references are a production layer: the volume has 33 and PP has
none, no .png is a member, and an unresolvable image path is NOT a text defect. The book's present is
2026. Expect the instrument to be wrong before the book — that fired twice in chat 125, zero times in
124, four in 123, three in 122, six in 121. Give every negative claim its own witness and state what a
sweep covered. Record the passes as well as the failures, or a class will look worse than the book is.
Index each volume once rather than rescanning it per phrase. Close the section read before the next
opens. At close: bank both goldens with gate.py bank, write W-165 ending with a blank line, build
BUILD155 with close.py (reverse must recover dad6b9fb…), write HANDOFF-79 BEFORE the final
verification, and begin the close with at least eight tool calls left. No corrections, no Register
entries, no TASK 1 until the review closes. Handoff at 90–95% of context or on a closed section read —
never earlier, never mid-section. Timeout on every call. Delete-only calls for pycache, never chained
to any other command. Never copy over an existing file."
