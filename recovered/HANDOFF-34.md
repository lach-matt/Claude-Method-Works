# HANDOFF-34 — The Method 1.6 — chat 81 → chat 82 (supersedes HANDOFF-33, written the same chat after M's ruling)

- Written from **chat 81** for **chat 82**. Live files: **BUILD90 main** (unchanged since chat 62) and **BUILD109 compendia** (= BUILD108 + W-119 + the chat-81 ruling appended to RULINGS-R2.md). Register **1 to 1792** (unchanged). W-118 and W-119 ARE seated; chat 82 seats nothing at open and writes W-120 at close. **HANDOFF-33 and BUILD108 are retired unused** — they were presented before M's ruling arrived and their next-work section carries the retired cadence.
- **Rulings and discipline:** the member `RULINGS-R2.md` (now 6,041 B · 30 lines) governs and is append-only; **read it at open, and read its last block first** — M ruled in chat 81 and the ruling changes the shape of every segment from here on. **Deferred cross-chapter items:** the member `DEFERRED.md` (now 31,167 B · 93 lines, with chat 81's block) governs; do not re-derive. **Findings carried to R3:** the READ-chNN.md members (READ-ch13h, READ-ch13i are chat 81's) and W-101…W-119 in WORKING-REGISTER.md; nothing is restated here.
- **Carried state to discard (Ruling 41):** the standing prompt block's "Phase 0–4" Löwdin/three-body plan is executed carried state (MEASURED in W-107, re-confirmed in chats 75–81: Chapters 35–36 at main-member L9716/L9892, Register 1701–1724); do not re-open it or put it to M. Project knowledge holds BUILD12/BUILD53 only — list it, never read those bundles. Retired handoffs are gone from Drive; never fetch or cite one. Never add HANDOFF-NN.md as a member.

## §0 Gate (each step its own tool call under `timeout 280`; any FAIL stops the chat with a report)

1. `ls -la /mnt/user-data/uploads /mnt/user-data/outputs /home/claude` (uploads holds this file); `recent_chats` confirms the latest chat is 81.
2. Fetch both bundles by title: `Google Drive:search_files` with `title contains 'BUILD109_compendia' or title contains 'BUILD90_main'`, pageSize 5, excludeContentSnippets; `Google Drive:download_file_content` on each fileId. Both spill to `/mnt/user-data/tool_results/<id>.json`; note the two paths (in chats 75–81 the spills left one-line pointers in context; the gate cost ≈ 15 % of the chat, MEASURED).
3. Bootstrap (decode, verify, extract — the only step outside gate.py, because gate.py is a member):

```
timeout 280 python3 - <<'EOF'
import json, base64, hashlib, re, os
EXP={'main':'49065309b0c4fe8e055f693aed295cca','comp':'2318980803bf6453c46d04a92f2e44ae'}
NAME={'main':'/home/claude/The_Method_1_6_BUILD90_main_and_register.md','comp':'/home/claude/The_Method_1_6_BUILD109_compendia_papers_audits.md'}
SPILL={'main':'/mnt/user-data/tool_results/<main-id>.json','comp':'/mnt/user-data/tool_results/<comp-id>.json'}
for tag in ('main','comp'):
    b=base64.b64decode(json.loads(json.load(open(SPILL[tag]))[0]['text'])['content'], validate=True)
    m=hashlib.md5(b).hexdigest(); print(tag, f'{len(b):,} B', m, b.count(b'\n'), 'lines', 'OK' if m==EXP[tag] else 'FAIL'); assert m==EXP[tag]
    assert not os.path.exists(NAME[tag]); open(NAME[tag],'wb').write(b)
os.makedirs('/home/claude/members'); n=0
for tag in NAME:
    for m in re.finditer(rb'^<<<FILE: (.+?)>>>\n(.*?)<<<END FILE: \1>>>\n', open(NAME[tag],'rb').read(), re.S|re.M):
        p='/home/claude/members/'+m.group(1).decode(); assert not os.path.exists(p); open(p,'wb').write(m.group(2)); n+=1
print('members extracted', n)
EOF
```

   Expected: main 1,983,081 B · 49065309b0c4fe8e055f693aed295cca · 18,470 lines; compendia **3,754,164 B · 2318980803bf6453c46d04a92f2e44ae · 43,694 lines**; 312 members extracted (2 + 310).
4. `python3 /home/claude/members/gate.py census` → `OK census: DEFECT-CENSUS.tsv byte-identical` (must run before any instrument: it refuses if a pycache or a scratch census exists).
5. `python3 /home/claude/members/gate.py run --core` → five `OK` lines (tower-2, kinds, minmax, r2-tools-constants, extent — the tower 976/1,654/2,535/13,585/70,905/199,130, the kinds 1565 · 1356 · 499 · 149, the min/max figures, the constants sites and the "1 to 1792" sites are the banked goldens; a diff prints only where the run differs).
< truncated lines 34-42 >

**The computable batch (r2-ch13j.py) — what is already known to be in it.** §14.5.8's family table L3814–3818 (full box 3ᵈ seeding 4 · 6 · 8 · 10 · 12 at **2d**; the down-set law **d + c − 1** and the box law **d + c − 2**) and its seed-table L3826–3829 (Λ₈ 976 · 11 · 7, Λ₉ 1,654 · 12 · 9, Λ₁₀ 2,535 · 13 · 9). §14.5.9's L3928–3932 table across prune / cover / +prune / random / reverse / LB / EXACT / spread, at Λ₈, the down-set d = 4 c = 4, and full boxes 3³ and 4³. §14.5.10's stable core of four and its "not necessary" claim, against Register L2225's withdrawal. §14.5.12–§14.5.14's position claims. **Every one of these is a seed size, and §14.5.9 L3919 says in the volume's own words that every seed size §14.5.7 and §14.5.8 report came from one heuristic and was wrong — so re-measure all of them with r2-ch13h §3's exact set-cover solver, never with prune-greedy.** That solver is the single most valuable instrument in the bundle for this section: envelope steps plus value slots as elements, two exact reductions (200 → 27 elements, 976 → 59 non-dominated covers at Λ₈), branch and bound; it settled seed(Λ₈) = 7 exactly in 38 s. Batch the stages and families through it in one instrument rather than one per subsection.

**The prose batch (r2-ch13k.py) — the model is r2-ch13i.** Pointer census resolved to the **claim**; every figure grepped for its other sites across all members; attributions and citation chains; self-description; vocabulary collisions. Known targets already measured: §14.5.10 L3961 (`§14.5.7 records that zero cells are forced`) and L3979 (`§14.5.7 stands unamended`) both report the content of a section with no body (13i-03), as does §14.5.8 L3812; DEFERRED carries §14.5.9's `reverse = 40` and `spread = 33` (prune in descending order measures **11**, so 40 is a different algorithm — identify it before recording); §14.5.8 L3821's `Λ₈ compresses 89×` and L3836's Carathéodory gloss inherit 13h-04 and 13h-05; L4154–4160's family table (S 7, A 4, F 3, G 4) is a self-count to re-derive. Instrument the census, do not hand-list it — a hand-list is a site list carried, and this project has found one short four times.

**Then continue by section read:** Chapter 15 (L4158–), Chapter 16, Chapter 17. Two batches each. Watch for the patterns chats 77–81 established: a Register entry restating a section may carry different figures — measure both, withdraw neither; a withdrawn figure may still stand at other sites — the later line governs, record the residue; a section may attribute a proof or a count to a neighbour that does not contain it (13d-01, 13h-01, 13i-04) — resolve every pointer to the claim, never to the heading alone; a claim may be true while the evidence printed for it is not (13g-01, 13e-04, 13h-04); and a rounded ratio may be printed with the word *exactly* (13h-04) — check the arithmetic of every compression, percentage and ratio, not only the counts.

**Per section read (close it — files written, md5s measured, both goldens banked — before the next opens):** `python3 members/r2-tools.py lines|pointers|figures|census|layout A B` over the whole section (its pointer regex is case-sensitive and does not know Appendix A's item numbers: grep lowercase `register NNN` and `A.N` separately and resolve by hand — a bare `A.2` or `§4.6` reported UNRESOLVED is usually an Appendix A item or a Chapter 4 protocol row, not a defect); read every line; census the claims into the two kinds, writing the census into the READ file so the batches are auditable against it; then the two batches in `members/r2-chNN.py` (next names r2-ch13j.py computable, r2-ch13k.py prose), importing r2lib by path:

```
import importlib.util, os; H=os.path.dirname(os.path.abspath(__file__))
s=importlib.util.spec_from_file_location('r2lib', os.path.join(H,'r2lib.py')); r2lib=importlib.util.module_from_spec(s); s.loader.exec_module(r2lib); T=r2lib.load_tower()
```

r2lib carries build9/tarjan/analyse (r2-ch12f), is_tree, closure/factor_q/ci/components/separates (r2-ch12i), support/direct/composed (r2-ch12j), mi/ci_sets/gG_index (r2-ch12k), lam9p, read_member, md5, the Λ₉/Λ₉′ and (g, G) constants. **Note `load_tower()` returns the tower-2 MODULE, not a dict — call `T.L8()`, `T.L9()`, not `T[8]`.** Not yet lifted (copy verbatim from the member with a provenance comment when next needed — DEFERRED lists them): r2-ch12c/e/g/h's functions; chat 75's closure_chunked/fixed_point/length/fibre_check/criterion; chat 76's terms/new_terms/jc_values (r2-ch12q); TERMS/L8_at/phi_at/**Rbox** (r2-ch12r — copied a third time into r2-ch13h as **Rset**, the set-returning form; lift both together); chat 77's stage-composability sets (r2-ch12w); chat 78's first_fail and failing_pairs (r2-ch12y — note r2lib.closure counts **ordered** pairs including self, so halve for the unordered counts the book prints); chat 79's `sections(X, w)` and `R(X)` / `failing(X, op)`; chat 80's `Rn(X)` (r2-ch13e) and r2-ch13f's `sections(X, w)` at 8–13 coordinates with its interval Box/binding measure; and chat 81's **exact set-cover seed solver**, `closure_mask` (the bitmask box sweep for small ambients, validated against Rset on every non-empty subset of the 8- and 9-cell ambients) and `prune` (prune-greedy against a fixed target) — all three in r2-ch13h. **Batching makes the lifting overdue: a batch instrument that copies four functions verbatim is worse than r2lib carrying them. Lift what the batch needs, verbatim, with provenance, and record the lift in DEFERRED.** The tower's sides, for any width: A = (n, ℓ, k, 2S, 2J_c, 2K, 2J) at indices 0,1,2,7,10,11,12; B = (e, f, g, 2S′, v) at 4,5,6,8,9; the base q at 3. Write READ-chNN.md (the claim census first, then A deviations with both texts, B verified, C incidental) and CENSUS-CLOSURES-chNN.tsv (id, verdict, reason; A vocabulary; header only when no row is in range). Instruments must print no wall-clock time (time a run in the shell with `date +%s` if needed). **A batch instrument over ~200 s must be split into two calls, not trimmed of coverage** — never build a set inside a per-cell comprehension, and prefer the box sweep (Rbox/Rset) over the pairwise fixed point (Rn) whenever a set exceeds ~2,000 cells, since Rn's cost is quadratic in the closure, not the input.

## Close of chat 82

1. `rm -rf /home/claude/members/__pycache__` (delete-only call); `python3 members/gate.py bank r2-ch13j …` for each new instrument (writes r2-ch13j.out; refuses to overwrite), then a **separate** delete-only pycache call.
2. Write /home/claude/W-120.md: begins `### W-120 —`, ends with a blank line; records the gate, each segment, the close estimate.
3. `python3 members/close.py --old /home/claude/The_Method_1_6_BUILD109_compendia_papers_audits.md --new /home/claude/The_Method_1_6_BUILD110_compendia_papers_audits.md --w /home/claude/W-120.md [--append DEFERRED.md /home/claude/deferred-add.md] --members members/READ-ch13j.md members/CENSUS-CLOSURES-ch13j.tsv members/r2-ch13j.py members/r2-ch13j.out …` — `--append` must precede `--members` (which consumes every later argument). It appends W-120 and the members, rewrites MANIFEST.tsv, asserts only WORKING-REGISTER.md, MANIFEST.tsv (and an appended member) changed, and must print `reverse recovers md5 2318980803bf6453c46d04a92f2e44ae  == old: True` before writing. Never edit a seated member in place. Note that close.py rewrites the bundle only — the extracted `/home/claude/members/` copies stay at their pre-close state, so `gate.py manifest` will report FAIL on exactly the changed members after a close; verify appends by reading the new bundle, not the members directory, and **print only the fields you need** — a whole-member dump costs several per cent of context.
4. HANDOFF-35 in this form (update: build numbers, md5s of the compendia bundle, MANIFEST.tsv, WORKING-REGISTER.md and DEFERRED.md, the member count, the run list, next work, the prompt). `ls -la /mnt/user-data/outputs` before copying; never copy over an existing file; present HANDOFF-35.md first, then BUILD110 and the READ files.
5. Handoff at 90–95 % of context or on a closed segment — never earlier, never mid-segment; begin the close when the remaining context would not fit a segment plus the close (≈ 12 % measured in chats 78–81; a segment ≈ 12–22 % measured in chat 81). No corrections, no Register entries, no TASK 1 until the review closes.

## M's Drive actions

- **UPLOAD** to Materials (`1XX_5f3PZB6tjRtiZ2GIgd8Tt7i_4GgkY`): `HANDOFF-34.md` and `The_Method_1_6_BUILD109_compendia_papers_audits.md` (23189808…).
- **RETIRE:** HANDOFF-33 and BUILD108 **unused, now** — they were superseded within the chat that wrote them and must not be uploaded; HANDOFF-32 and BUILD107 once BUILD109 gates in chat 82; HANDOFF-31, BUILD106 and any earlier compendia builds if still present.
- **KEEP:** BUILD90 main, tower-2.py, chat65/66/67-instruments.tar.gz, SWEEP-B-C-chat67.md, convert.py, the certificates, OWED-REGISTER-EXPANSIONS.md, OWED-EXPANSIONS-2.md, covers8.json, factor.py.

## Prompt for chat 82

"Chat 82. READ EVERYTHING BEFORE YOU DO ANYTHING. Live files: BUILD90 main (1,983,081 B, md5 49065309b0c4fe8e055f693aed295cca) and BUILD109 compendia (3,754,164 B, md5 2318980803bf6453c46d04a92f2e44ae, 43,694 lines, 310 members). List uploads, outputs and /home/claude first. Run HANDOFF-34's §0 gate in full and in order — fetch both bundles by title, bootstrap (decode, md5, extract), then gate.py census, run --core, manifest, run r2-ch13h r2-ch13i, cert 82; any FAIL stops the chat with a report. Read RULINGS-R2.md — its last block is M's chat-81 ruling and it changes the cadence — and DEFERRED.md, from the members. The standing block's Phase 0–4 (Löwdin/three-body) plan is executed carried state; discard it per Ruling 41. HANDOFF-33 and BUILD108 are retired unused. Then continue Phase R2 under the new cadence: read main §14.5.8 through §14.6 (L3810–L4157) as one section read, census its claims into computable and prose, then run exactly two instrument batches — r2-ch13j for the computable claims, re-measuring every seed size with r2-ch13h's exact set-cover solver rather than prune-greedy since §14.5.9 L3919 declares every seed size §14.5.7 and §14.5.8 report to have come from one wrong heuristic, and r2-ch13k for the prose claims on the model of r2-ch13i. Then Chapter 15, Chapter 16, Chapter 17, two batches each. Resolve every pointer to the claim and not the heading, measure a claim and its stated witness separately, check the arithmetic of every ratio and compression as well as every count, close each section read before the next opens. At close: bank both goldens with gate.py bank, write W-120, build BUILD110 with close.py (reverse must recover 23189808…), write HANDOFF-35. No corrections, no Register entries, no TASK 1 until the review closes. Handoff at 90–95 % of context or on a closed section read — never earlier, never mid-section. Timeout on every call. Delete-only calls for pycache, never chained to gate.py bank. Never copy over an existing file."
