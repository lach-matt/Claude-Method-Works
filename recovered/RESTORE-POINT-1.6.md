# RESTORE POINT — THE METHOD 1.6

`restore-point-1.6.tar.gz` — built with **six audit sets passing**.

## State

| | |
|---|---|
| book | `The Method 1.6.md` → **The Method 1.6.pdf**, 249 pages, 33 figures |
| register | 165–669, in `REGISTER-DATA.md`, **not in the book** |
| mathematics | **197 objects · 191 settled · 6 unfinished · 3% open** (from 31% at the start) |
| seed | **14 roots — unmoved across every cycle** |
| ratio | 14.1 : 1 |

## The five documents

| | pp | generated by | holds |
|---|---|---|---|
| **The Method 1.6** | 249 | `The Method 1.6 press.py` + `pagemap.py` | the argument, Appendix A's proofs, Chapter 32's self-application, and pointers to the register |
| **The Register** | 53 | `register_gen.py` ← `REGISTER-DATA.md` | every entry ever written |
| **The Index of Indices** | 29 | `indices.py` + `fig_indices.py` | every index on or beside Λ, four figures, Λ₈'s 976 cells |
| **Mathematical Compendium** | 26 | `compendium.py` ← `mathreg.py` | 197 objects, the seven languages and why each is necessary |
| **Spectra Compendium** | 5 | `spectra.py` ← `SPECTRA-DATA.tsv` | the 133 channels, eleven columns |

## To restore

```
tar xzf restore-point-1.6.tar.gz -C /home/claude/work/
cd /home/claude/work
python3 "The Method 1.6 press.py"          # pass one
cp /mnt/user-data/outputs/"The Method 1.6.pdf" .
rm -rf .zeno/pagemap.json && python3 pagemap.py    # heading -> page
python3 "The Method 1.6 press.py"          # pass two, with page numbers
cp /mnt/user-data/outputs/"The Method 1.6.pdf" .
python3 compendium.py   > COMPENDIUM.md
python3 spectra.py      > SPECTRA.md
python3 register_gen.py > REGISTER.md
python3 indices.py      > INDICES.md
python3 press_compendia.py
```

**The book needs two passes.** A page number does not exist until the document is laid out, so
`pagemap.py` reads a built PDF and the press reads `PAGEMAP.tsv` on the next build. Without the file
the contents renders unnumbered and the build still succeeds.

## The six audit sets

```
rm -rf .zeno/audits.json           && python3 The_Method_1_6_audits.py
rm -rf .zeno/appendix_audit.json   && python3 appendix_audit.py
rm -rf .zeno/criterion_probe.json  && python3 criterion_probe.py
rm -rf .zeno/cross_audit.json      && python3 cross_audit.py
rm -rf .zeno/compendium_audit.json && python3 compendium_audit.py
rm -rf .zeno/artefact_audit.json   && python3 artefact_audit.py
```

Expect `ALL TWENTY-TWO PASS`, `ALL APPENDIX CHECKS PASS`, `CRITERION PROBE PASSES`, and
`PART 1/2/3 PASSES`.

**Ten of the twenty-two test the index; twelve test the book.** §3.0 records which.

## And a seventh check that has no script

**Look at the rendered pages.** Every layout fault found in this version — a contents missing 202
subsections, a contents with no page numbers, page numbers wrapping to their own line, tables laid
out with equal column widths, a table centred so wide it fell off both edges, identifiers broken
mid-word because code renders in a wider font — **was invisible to all six audit sets and obvious at
a glance.**

```
pdftoppm -png -r 70 -f 1 -l 2 "The Method 1.6.pdf" shot
```

**Registers 664–669 are that lesson.** A document has to be looked at, and no amount of reading its
text is the same thing.

## The six unfinished

**Four need the violation index's cells** — `W.core9`, `W.onehot`, `W.supp`'s failure count, and
`W.frontier`'s application. *`vi_best.json` holds distance 29 with cells exactly 2,370; the whole
residual is on the NEC interior, +16 on NEC ≥ 2 and −9 on NEC ≥ 3, missing in opposite directions.
Eight alphabets have failed to move it. Resume from that file, never blind — register 591.*

**Two need one line each** — `B.hstar`'s w(h) and V(h); and how the LS and jj brackets were derived,
which is *not* the looseness convention.

**One is correctly open** — `M.C2`, half-sided modular inclusion on a non-expanding horizon with no
Killing field.

## The spectra collection

**3 species banked** — Ne I, Ne II, Be I, in `spectra_raw/`. **26 reachable, 6 not on the route.**
`SPECTRA-REMAINING.md` carries the list, the priority order, and the three retrieval findings:

- the ASD query interface is robots-disallowed on every mirror; the **Handbook** at
  `physics.nist.gov/PhysRefData/Handbook/Tables/[element]table5.htm` is open
- the ASCII variant is a fifth the size and **unreachable** — it is linked behind an image
- the fetcher's URL index is **recency-limited**, so `element_name.htm` must be re-fetched
  immediately before each element

**`B.adm` stays blocked whatever is collected: the Handbook prints no uncertainties.**
