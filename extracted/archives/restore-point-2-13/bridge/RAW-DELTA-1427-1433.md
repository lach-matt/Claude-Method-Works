## DELTA FROM restore-point-1_6p.tar.gz (register 1426) -> 1433

### Six code changes

1. `ladder_index.py` — add the two X-ray ladders at the **subvalence** seat (index 2): Moseley as `(2, counting, across)`, the Kα doublet as `(2, coupling, within)`. Result: 14 ladders, 9 cells, E = 0.

2. `indices.py` — after the paragraph ending "Janet's 1928 ordering, chosen for shell filling, happens to close", append: Janet's zero is **forced, not earned** — (n+ℓ, Z) holds one cell per element so no envelope can bind; the same 118 elements on **(Janet block, position within block)** give **118 cells, box 256, E = 2**, the two absent cells being block 8 at positions 31 and 32; the n+ℓ = 8 block is 5f+6d+7p+8s = 32 running Z = 89 to **120**, and shortening the last block by k gives E = k exactly. Janet 1929; OEIS A173592; the index reproduces his count rather than predicting it.

3. `zeno.py` — add `import sys` at the top, and append `, file=sys.stderr` to all eight `print(` calls in `step()` and the summary. The checkpoint log was landing inside `INDICES.md`.

4. `roundtrip.py` — delete the two progress-line regexes from `NORM`; keep only the date substitution. 5 of 5 still round-trip without them.

5. `INDICES-TAIL.md` — replace Λ_ladder's "Why it does not close" with **IT CLOSES**, and both summary tables' "Thirteen close" with **"Fourteen close"**, restating Λ_spectra's cause as the limit rather than the missing principal number.

6. `COMPENDIUM-TAIL.md` — two insertions: the C3 falsifier paragraph after the Nilsson narrowing, and the R 1426 limit correction in the Λ_spectra section (38/20, not 47/11).

### Seven registers

**1427 — Λ_ladder closes, E = 0 at nine cells, and the loop shuts where it opened.** Open since 1356. The X-ray ladders added and the seat was the only question. Only subvalence closes: nucleus 1, core 2, valence 1. Contingency: 90% of comparable nine-cell sets refuse, so the zero is earned. The circularity is resolved from outside rather than by the closure — Λ_PCA excluded the core independently, on different coordinates, before this was computed. The physics agrees: a Kα line is not a property of the 1s shell but a transition *between* shells, so the ladder's seat is where the transition spans, not where the hole sits. Fourteen ladders, nine cells, E = 0. The defect at the subvalence seat demanded Λ_xray, and Λ_xray filled it.

**1428 — B2a resolved: the five shared cells are four findings and one separation.** Two pairs are **one object** — isotonic fixes N and isobaric fixes A with A = Z + N; isoelectronic fixes Nₑ and the walk fixes c with Z = Nₑ + c − 1. That is *charge is one symbol in three positions* one level up. Two are separated by axes already present: isotopic is species and the isomeric is state; Rydberg fixes the species while the ℓ-ladder fixes species *and* n. The fifth was the real ambiguity — the term ladder and the outer-j ladder.

**1429 — the term and j ladders are five tower stages apart, nested not parallel.** Transition happens down and inward; the coupling hierarchy resolves configuration, then term, then level, and the tower adds its axes in that order. The term ladder moves 2S, Λ₈'s own letter; the outer-j ladder moves 2J, arriving only at Λ₁₃. Adding the tower stage as a fourth axis separates that pair and only that pair — but costs closure, E going 0 → 1. So it is the discriminator, not an axis.

**1430 — C3, the nuclear claim survives its own falfier, by one shell.** The field fits κ and μ per mass region, so the corridor's contribution stands only if one region holds both orientations. **The 82–126 shell does**: 3p₃/₂ → 2f₅/₂ demands 4β + α > 0 and 1h₉/₂ → 2f₇/₂ demands < 0, both in oscillator shell N = 5. The other five pairs are one sense per shell — 8–20 positive, 28–50 positive, 50–82 both negative — so had 82–126 split the same way the result would have fallen. And 82–126 is exactly where κ and μ are quoted as a unit.

**1431 — C4, the refutation survives all four orderings.** The 21-constraint system rebuilt with 2f₇/₂ and 1h₉/₂ swapped, with 3p₃/₂ and 2f₅/₂ swapped, and with both: **feasible set empty in all four.** A second diagnostic in the same run is *discarded, not reported* — it counted senses by reordering each pair by list position and taking the sign of dA/dS, making the sense a function of the ordering just imposed.

**1432 — topic C is complete.** C1 the identity, C2 the screen, C3 the falsifier, C4 the swaps. Every test that could have retired the result has been run rather than argued. What remains is not a test but a construction: no form outside the Nilsson family has been proposed that passes the six-pair screen.

**1433 — F2c, the timing line fixed at source and the normalisation retired.** `zeno.py` printed its checkpoint log to stdout while `indices.py` writes its artefact to stdout, so the log landed in the file. All eight prints now go to stderr. And `roundtrip.py`'s normalisation is **removed** rather than kept as a safety net — 5 of 5 still round-trip, which is the stronger check. *A workaround that succeeds hides the defect it accommodates; retiring it is how you find out whether the repair took.*
