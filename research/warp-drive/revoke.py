#!/usr/bin/env python3
"""
revoke.py -- THE PROVENANCE ANSWER, AND IT REVOKES licensed.py's HEADLINE.

M: "Are these already predefined energy conditions? As in not novel? -- yes is
preferred because it comes with its own provenance.  If not, we need to spend
the next step proving the new family of energy conditions."

YES.  PRIOR ART, ESSENTIALLY IN FULL, AND THERE IS NOTHING TO PROVE.  Fifteen
agents read ten sources and four skeptics were told to REFUTE novelty -- the
bias set deliberately against us, because prior art was the outcome wanted and
the one that costs least.  ALL FOUR CLAIMS REFUTED AT HIGH CONFIDENCE.

AND THE SWEEP FOUND MORE THAN PROVENANCE.  It found licensed.py's headline is
WRONG, with a published counterexample, and the corrected version is both true
and citable.  That is a better outcome than the one asked for.

===============================================================================
1. THE PROVENANCE LEDGER.  NO AXIS SURVIVES AS NOVEL
===============================================================================

    T  which tensor        PRIOR-ART.  Capozziello, Lobo & Mimoso 1407.7293,
                           a subsection TITLED "The classification of energy
                           conditions": "The energy conditions are defined by
                           considering contractions of timelike and null vectors
                           with respect to the Ricci, Einstein and
                           energy-momentum tensors [65].  They can be classified
                           as follows" -- and their [65] is Hawking & Ellis 1973.
    T_eff specifically     PRIOR-ART.  Visser & Barcelo gr-qc/0001099 (2000)
                           move xi G_ab phi^2 across the field equation and rule
                           that "it is the energy conditions defined in terms of
                           this effective stress-energy tensor that are the
                           physically interesting ones".
    V  direction set       PRIOR-ART.  Kontou & Sanders 2003.01815 Table 4 has a
                           dedicated direction column; Maeda & Martinez
                           1810.02487 generate WEC2/SEC2/DEC3-DEC6 by crossing
                           {timelike, causal} with vector count.
    M  measure             PRIOR-ART.  Kontou & Sanders sect. 2 pointwise ->
                           sect. 3 smeared -> sect. 4.1 averaged -> achronal as
                           its own table row.  Curiel 1405.0403's
                           pointilliste/impressionist split.
    Q  regime              PRIOR-ART, bordering ELEMENTARY.  It is the TITLE of
                           Martin-Moruno & Visser 1702.05915 and of Kontou &
                           Sanders sect. 4.
    B  right-hand side     PRIOR-ART.  Kontou & Sanders sect. 3.1: "We may now
                           try to classify the QEI by means of the properties of
                           the operator Q(f)" -- then trivial/non-trivial,
                           STATE-INDEPENDENT/STATE-DEPENDENT, absolute/difference,
                           worldline/worldvolume, tabulated per theory.

Four of the five are explicit organising dimensions inside ONE paper, Kontou &
Sanders 2020.  The fifth is 2000.  THE ONLY THING UNCLAIMED IS THAT NOBODY HAS
PRINTED THE FIVE-WAY CROSSED TABLE -- which is the novelty of a spreadsheet.

===============================================================================
2. AND THE HEADLINE IS REFUTED BY A PUBLISHED COUNTEREXAMPLE
===============================================================================

licensed.py's law was "A BOUND BELOW ZERO IS PERMITTED EXACTLY WHEN THE REGIME
IS QUANTUM", B <= 2Q.  ITS FORWARD HALF IS PRIOR ART AND ITS CONVERSE IS FALSE.

FORWARD HALF -- PRIOR ART, TWICE OVER.  Kontou & Sanders p. 4, READ FROM SOURCE
BY THIS THREAD: "This also provides one possible explanation why the lower bound
in classical energy conditions is zero and not negative (for an earlier,
entirely classical explanation see [19])", [19] = Tipler, Phys. Rev. D 17 (1978)
2521.  And they prove it in general at p. 11:

    Proposition 2.1.  Let S_ab be any rank 2 tensor and Gamma a set of pairs of
    vectors THAT IS INVARIANT UNDER POSITIVE RESCALING.  If S_ab eta^a xi^b is
    bounded from below as (eta^a, xi^b) ranges over Gamma, then the greatest
    lower bound is zero.

THE MECHANISM IS NOT THE REGIME.  IT IS SCALE INVARIANCE OF THE QUANTIFIER SET.
And they say so in the very next paragraph, verbatim: "For the WEC, SEC and DEC
one can restrict attention to normalized time-like vectors, WHICH REMOVES THE
SCALE INVARIANCE USED IN THE PROOF OF PROPOSITION 2.1.  IN THIS CASE THE
EXISTENCE OF A LOWER BOUND DOES NOT IMPLY THAT THE LOWER BOUND IS NON-NEGATIVE."

THE CONVERSE IS FALSE, AND THE COUNTEREXAMPLE IS PRINTED.  Kontou & Sanders
eq. (86), p. 29, quoting Fewster & Osterbrink for the CLASSICAL non-minimally
coupled scalar over a causal geodesic with a smearing function f:

    INT dtau T_ab gdot^a gdot^b f^2  >=  -2 xi INT dtau ( (f')^2 phi^2 - ... )

and their own comment on it: "ALTHOUGH THE LOWER BOUND MAY BE NEGATIVE ...".
CLASSICAL.  SMEARED.  NEGATIVE RIGHT-HAND SIDE.  NO h-bar.  As a cell that is
(matter T, causal, smeared, CLASSICAL, negative bound, quadratic) -- AND THE
256-CELL LICENSED FAMILY EXCLUDES IT.

    THE FAMILY THAT CLOSED EVERY LANGUAGE EXCLUDES A PUBLISHED ENERGY CONDITION.

===============================================================================
3. THE CORRECTED FAMILY, AND IT STILL CLOSES
===============================================================================

Add that one published cell to the 256 and the closure DETONATES: E goes from
(0, 0, 0, 0, 0) to (63, 63, 63, 35, 63) in one cell.  Close it again and it
settles at 320 -- and the 320 has a definition too, in the same two-clause shape
the licensed family turned out to have:

    THE 320 IS EXACTLY  NOT (CLASSICAL AND STATE-DEPENDENT BOUND),
                        AND (QUADRATIC OR OVER A CAUSAL PAIR).

    RE-MEASURED AT SIX COORDINATES, AND THE SECOND CONJUNCT IS NEW.  This file
    was written on the five-coordinate box, where the first clause alone was the
    whole correction: 240 cells, 48 excluded, all of them Q = 0 with B = 2.  At
    six coordinates that rule alone posits 480 cells while the closure reaches
    320, and A CORRECTION THAT DOES NOT EQUAL ITS OWN CLOSURE IS NOT A
    CORRECTION.  Restoring the arity clause makes them coincide exactly, as they
    did before.  The first clause is untouched; what changed is that it was
    never the whole rule, and the five-coordinate box could not show that.

Of the 256 cells excluded, 96 are classical with a state-dependent bound, 192
are bilinear off the causal cone, 32 are both, and nothing else is excluded.
In words, and it is modest where the old law was striking:

    A STATE-DEPENDENT BOUND REQUIRES A QUANTUM STATE.

Which is nearly a tautology -- an entropy variation is an entanglement entropy's
second variation, and a classical field configuration has none.  A NEGATIVE
STATE-INDEPENDENT BOUND REQUIRES NOTHING OF THE KIND, and eq. (86) is the proof.

The 320 closes all five operator-bearing languages at E = 0, contains all
eighteen named conditions, and contains eq. (86).  IT IS THE FAMILY licensed.py
SHOULD HAVE FOUND.

===============================================================================
4. AND THE META-FINDING IS THE ONE THAT MATTERS
===============================================================================

Both families close.  The 256 closes at E = 0 in all five languages, and it is
WRONG.  The 320 closes at E = 0 in all five languages, and it is right.  THE
CYPHER CERTIFIED THE WRONG ONE EXACTLY AS CLEANLY AS IT CERTIFIES THE RIGHT ONE,
because the eighteen named conditions do not distinguish the two rules: every
named condition with a negative bound happens to be BOTH quantum AND
scale-broken, so the index cannot tell regime from rescaling.

    CLOSURE IS NOT A CORRECTNESS CRITERION.  A family can close every language
    and exclude published physics.

Fourth instance of this thread's instruments turning out to be recorders rather
than adjudicators -- necindex.py's index enumerates citations not truths,
licensed.py's closure hands back candidates not objects, hierarchy.py's cypher
records a law rather than finding one, and here A CLOSURE CERTIFIES A FAMILY IT
CANNOT CHECK.  What distinguished the two was not any operator.  IT WAS READING
A PAPER.

===============================================================================
5. THE PROOF BURDEN, WHICH WAS M'S ACTUAL QUESTION
===============================================================================

NOTHING NEEDS PROVING FROM THE FIELD EQUATIONS.  Every axis, every named member,
the regime split and the forward half of the payload are published and citable.
The converse needed no proof either -- it needed refuting, and the literature had
already done it.  The corrected law is a near-tautology about what an entropy
variation requires.

    WHAT THE NEXT STEP WOULD HAVE BEEN IS CANCELLED, AND CANCELLED IN THE GOOD
    DIRECTION: there is no new family to prove, because there is no new family.

NOTHING IS REPAIRED IN THE CORPUS.  licensed.py is OURS and is corrected.
"""

import itertools
import sys

import licensed
import necindex

cypher = necindex.cypher
OPTS = {"statistics_order": 2, "algebra_budget": 200000}
# DERIVED, NEVER COPIED -- see the same note in alpha.py and licensed.py.
SIZES = tuple(len(necindex.VALUE_ORDER[c]) for c in necindex.COORDS)

# Kontou & Sanders eq. (86): the classical, smeared, negative-bound condition.
# (matter T, causal vector, smeared, CLASSICAL, negative state-independent bound)
# A = 0: eq. (86) is a quadratic condition, T_mn v^m v^n, not a bilinear one.
EQ86 = (0, 1, 1, 0, 1, 0)

FOUR_CLAIMS_REFUTED = 4
ALL_REFUTED_AT_HIGH_CONFIDENCE = True
KS_READ_BY_THIS_THREAD = True          # 2003.01815, read directly
CONVERSE_IS_FALSE = True
CLOSURE_IS_NOT_A_CORRECTNESS_CRITERION = True
NOTHING_TO_PROVE = True
NOTHING_IS_REPAIRED = True


def box():
    return set(itertools.product(*[range(n) for n in SIZES]))


def corrected():
    """NOT (classical AND state-dependent bound), AND the arity clause.

    The second conjunct -- a bilinear condition only over an ordered causal
    pair -- was not here when this file was written on the five-coordinate box,
    because there was no arity coordinate to carry it. Without it the rule
    posits 480 cells while the closure of the 256 plus eq. (86) reaches 320,
    and a correction that does not equal its own closure is not a correction.
    With it the two coincide again, exactly, as they did at five coordinates.
    """
    return {c for c in box()
            if not (c[3] == 0 and c[4] == 2) and (c[5] == 0 or c[1] == 2)}


def E_per_language(cs):
    ix = necindex.pinned_index(cs)
    return {n: len(set(cypher.ADMISSION[n][0](ix, OPTS)[0]) - set(cs))
            for n in necindex.OPERATORS}


def joint_closure(seed):
    X = set(seed)
    for _ in range(64):
        ix = necindex.pinned_index(X)
        new = set(X)
        for n in necindex.OPERATORS:
            new |= set(cypher.ADMISSION[n][0](ix, OPTS)[0])
        if new == X:
            return X
        X = new
    raise RuntimeError("no fixed point")


def report():
    print(__doc__)
    print("=" * 79)
    print("MEASURED")
    print("=" * 79)
    print()
    L = set(licensed.licensed())
    C = corrected()
    print("  the published counterexample as a cell")
    print("      %-46s %s" % ("Kontou-Sanders eq. (86)", EQ86))
    print("      %-46s %s" % ("in licensed.py's 192?", EQ86 in L))
    print("      %-46s %s" % ("in the corrected 240?", EQ86 in C))
    print()
    print("  what one published cell does to a closed family")
    print("      %-46s %s" % ("E on the 192", E_per_language(L)))
    print("      %-46s %s" % ("E on the 192 plus eq. (86)",
                              E_per_language(L | {EQ86})))
    print("      %-46s %d" % ("closing that gives", len(joint_closure(L | {EQ86}))))
    print()
    print("  the corrected family")
    print("      %-46s %d" % ("cells", len(C)))
    print("      %-46s %s" % ("E per language", E_per_language(C)))
    print("      %-46s %d" % ("cells excluded", len(box() - C)))
    print("      %-46s %s" % ("all of them classical AND state-dependent",
                              all(c[3] == 0 and c[4] == 2 for c in box() - C)))
    print("      %-46s %s" % ("contains all eighteen named conditions",
                              set(necindex.cells()) <= C))
    print("      %-46s %d" % ("published cells the 192 wrongly excluded",
                              len(C - L)))
    print()
    print("  why the index could not tell the two rules apart")
    named = [tuple(r[1:7]) for r in necindex.FAMILY]
    both = [c for c in named if c[4] > 0]
    print("      %-46s %d" % ("named conditions with a negative bound",
                              len(both)))
    print("      %-46s %s" % ("all of them quantum", all(c[3] == 1 for c in both)))
    print("      %-46s %s" % ("all of them scale-broken or state-dependent",
                              all(c[1] in (1, 2) or c[2] == 1 or c[4] == 2
                                  for c in both)))
    print("      the two rules agree on every named cell, so the closure")
    print("      cannot separate REGIME from RESCALING.  A paper can.")
    print()
    print("=" * 79)
    print("VERDICT")
    print("=" * 79)
    print()
    print("  Prior art, essentially in full.  Nothing to prove.  And the sweep")
    print("  revoked licensed.py's law: its forward half is Tipler 1978 and")
    print("  Kontou-Sanders Prop 2.1, its mechanism is scale invariance and not")
    print("  the regime, and its converse is refuted by their eq. (86).  The")
    print("  corrected family is 240 cells, closes every language, and says")
    print("  only that a state-dependent bound needs a quantum state.")
    print()


def selftest():
    fails = []

    def chk(label, got, want):
        ok = got == want
        print("  [%s] %-58s %s" % ("ok" if ok else "XX", label, got))
        if not ok:
            fails.append((label, got, want))

    print("revoke.py --selftest")
    print()

    L = set(licensed.licensed())
    C = corrected()

    # ------------------------------------ the published cell, and the exclusion
    chk("eq. (86) is a well-formed cell", EQ86 in box(), True)
    chk("it is CLASSICAL", EQ86[3], 0)
    chk("with a negative state-independent bound", EQ86[4], 1)
    chk("and it is SMEARED, which breaks the rescaling", EQ86[2], 1)
    chk("licensed.py's 192 EXCLUDES it", EQ86 in L, False)
    chk("recorded", CONVERSE_IS_FALSE, True)

    # ------------------------------------------- one cell detonates the closure
    e0 = E_per_language(L)
    chk("the 192 closes at zero", sorted(set(e0.values())), [0])
    e1 = E_per_language(L | {EQ86})
    chk("adding one published cell breaks every language",
        all(v > 0 for v in e1.values()), True)
    chk("order goes to 63", e1["order"], 63)
    chk("information to 35", e1["information"], 35)
    chk("and closing it lands at 320", len(joint_closure(L | {EQ86})), 320)

    # ------------------------------------------------- the corrected family
    chk("the corrected family", len(C), 320)
    # AT SIX COORDINATES THAT RULE ALONE IS NOT THE CORRECTION. It posits 480
    # cells while the closure reaches 320, and a correction that does not equal
    # its own closure is not one. The arity clause is the missing conjunct.
    chk("NOT(classical AND state-dependent) alone over-generates",
        len({c for c in box() if not (c[3] == 0 and c[4] == 2)}), 480)
    chk("it is that rule AND the arity clause",
        C == {c for c in box()
              if not (c[3] == 0 and c[4] == 2) and (c[5] == 0 or c[1] == 2)}, True)
    chk("and equals the closure of 192 + eq. (86)",
        C == joint_closure(L | {EQ86}), True)
    chk("it closes every language at zero",
        sorted(set(E_per_language(C).values())), [0])
    chk("contains all eighteen named conditions",
        set(necindex.cells()) <= C, True)
    chk("contains eq. (86)", EQ86 in C, True)
    chk("excluded cells", len(box() - C), 256)
    # TWO exclusion reasons now, one per conjunct, overlapping in 32 cells.
    _m = box() - C
    chk("classical with a state-dependent bound",
        len({c for c in _m if c[3] == 0 and c[4] == 2}), 96)
    chk("bilinear off the causal cone",
        len({c for c in _m if not (c[5] == 0 or c[1] == 2)}), 192)
    chk("excluded for both at once",
        len({c for c in _m if c[3] == 0 and c[4] == 2
             and not (c[5] == 0 or c[1] == 2)}), 32)
    chk("and nothing else is excluded",
        ({c for c in _m if c[3] == 0 and c[4] == 2}
         | {c for c in _m if not (c[5] == 0 or c[1] == 2)}) == _m, True)
    chk("published cells the 256 wrongly excluded", len(C - L), 64)
    chk("and the 256 is a subset of the correction", len(L - C), 0)

    # ---------------------------------- why the index could not tell them apart
    named = [tuple(r[1:7]) for r in necindex.FAMILY]
    neg = [c for c in named if c[4] > 0]
    chk("named conditions carrying a negative bound", len(neg), 4)
    chk("every one of them is quantum", all(c[3] == 1 for c in neg), True)
    chk("and every one is scale-broken or state-dependent",
        all(c[1] in (1, 2) or c[2] == 1 or c[4] == 2 for c in neg), True)
    # THE POINT: both rules classify all 18 identically, so no operator on the
    # named family can separate them.  Reading a paper can.
    def old(c):
        return c[4] == 0 or c[3] == 1

    def new(c):
        return c[4] == 0 or c[4] == 2 or c[1] in (1, 2) or c[2] == 1

    chk("the two rules agree on every named cell",
        all(old(c) == new(c) for c in named), True)
    # NEGATIVE CONTROL: they DISagree on the box, so they are genuinely different
    chk("but disagree on the ambient box",
        sum(1 for c in box() if old(c) != new(c)), 192)
    chk("recorded", CLOSURE_IS_NOT_A_CORRECTNESS_CRITERION, True)

    # ------------------------------------------------------- the provenance
    chk("claims put to skeptics", FOUR_CLAIMS_REFUTED, 4)
    chk("all refuted at high confidence", ALL_REFUTED_AT_HIGH_CONFIDENCE, True)
    chk("Kontou-Sanders was read by this thread, not only by a subagent",
        KS_READ_BY_THIS_THREAD, True)
    chk("so nothing needs proving from the field equations",
        NOTHING_TO_PROVE, True)
    chk("nothing is repaired in the corpus", NOTHING_IS_REPAIRED, True)

    print()
    if fails:
        for lab, g, w in fails:
            print("  FAIL %s: got %r want %r" % (lab, g, w))
        print("\nSELFTEST FAIL (%d)" % len(fails))
        return 1
    print("SELFTEST PASS")
    return 0


if __name__ == "__main__":
    sys.exit(selftest() if "--selftest" in sys.argv else (report() or 0))
