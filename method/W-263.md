### W-263 — thirty-three goldens re-banked, and the pointer class that no instrument here measures

- **THE CENSUS, STRUCTURAL AGAIN.** Registers 1871–1874 moved **33 of the 85 live goldens**. The Register's diff is **five front-matter replacements and one insertion at L6925 of 6,941 lines** — the end. **Nothing precedes the insertion, so no line reference can have moved**, and the shift class is ruled out rather than checked. The five replacements are the count classes; every remaining movement is a site tally raised by what the four entries quote.
- **THREE OF THE FOUR ENTRIES ARE THE SAME SHAPE, AND IT IS ONE NO INSTRUMENT HERE MEASURES.** §23.10.2's *"recorded at 96–98"* resolves to three supersession stubs forwarding to an entry about time entering Λ. §12.11.1's *"the ⅔ §12.11.1 states as its nominal value"* resolves to §12.11.1, which states no nominal value in its 330 lines. A.15's *"Register 230"*, seated at 1861 three builds ago, resolves to an entry about Appendix D's *kind* coordinate. **`pointers.py` scores all three RESOLVED, and is right to**: its question is whether the address exists, and all three addresses do. **What fails is the content at the far end.** That is `DOCKET.md`'s class *"targets that say nothing of the claim"*, which Phase 3 carries — and it now has **three measured instances instead of a description**, which is what a prose pass needs to work from.
- **AND THE CLASS HAS A DETECTABLE SIGNATURE, WHICH IS WORTH RECORDING BEFORE PHASE 3 OPENS.** In all three, the citing text names *what* the target establishes. **That is checkable against the target's own words without reading either for sense** — a successor to `pointers.py` could score the claim as well as the address wherever a citation carries a description. Not built here; named so it is a decision rather than an omission.
- **`r2-reg7a2` HELD AT 22 OK** for the seventh build running, and the census is a fixed point at BUILD257.
- **PHASE 2 STANDS AT 32 DONE, 1 PART, 13 OPEN**, from 3 done when this stretch opened. **Eight of the thirteen reduce to the three rulings M has not given.** The five that remain measurable are `A-F4` (register 1580's corridor census against `slopeaxis.py`), `E-015` (successors for 392/393 and 625's chain to 623), `E-074` (§21.6's envelope-step *five*, where 547 and 548 are said to disagree), `F-218` (D.5.9's E figures and the 3B.five precedent) and `F-266` (the eighteen register-read findings reg1-01 to reg13-01, which need a disposition each).
- **Re-banked BUILD257 → BUILD258 by running each instrument**, under `close_rebank.py`'s guards: change set proved to be `{WORKING-REGISTER.md, MANIFEST.tsv, the 33 goldens}`, reverse guard recovering BUILD257's own md5.

