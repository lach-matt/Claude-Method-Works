
## Chat 153A --- deferred out of R3's first two executions (staged loose; seat with close.py --append DEFERRED.md method/DEF-153A-PENDING.md, then delete the file)

1. **A correction entry seeds a permanent arithmetic false positive, and R3 will accumulate them.** Register entries are append-only and a superseded state is preserved beside the claim that overturned it, so an entry recording an arithmetic repair *quotes the defective figure*. MEASURED after item 153-01: `tools/arith.py --roster volumes` now scores **reg L6623 DISAGREE, `146 of 163 = 92%`** --- that line is entry 1795's own quotation of the text it corrected. The repair is right, the entry is right, and the instrument is right; the verdict is an artefact of all three.
2. **Consequence for the remaining docket.** Every future arithmetic-class repair adds one such row. The audit's DISAGREE count therefore **stops being a defect count** the moment R3 runs, and must never again be quoted as one without subtracting the correction entries. After 153-01 the two DISAGREE rows are **both artefacts**: reg L6623 (entry 1795's quotation) and main L1413 (the volume's own ARITHMETIC audit fixture, 153-02).
3. **Not repaired, and the instrument NOT changed (G0c).** The obvious move --- teach `arith.py` to skip Register entry bodies, or to skip lines inside an entry marked *(a correction.)* --- is a design decision that would silence real defects in the Register's own arithmetic, which is exactly what RUL-128 item 3 (ii) requires to be re-derived. **It is recorded here and left to M's ruling or to a segment of its own with its own selftest fixture.** The cheap alternative, if one is wanted, is a `--exclude-corrections` flag that is off by default and named in every report that uses it.
4. **The same shape will appear in the pointer audit.** A correction entry that cites the pointer it repaired will resolve or fail to resolve on the old target. No instance yet; recorded before it is met.
5. **Instrument fixtures follow the corpus, and the following moved at this close, each recorded rather than tuned away:** `arith.py`'s CORPUS fixture `146 of 163 = 92%`/DISAGREE became `146 of 163 = 90%`/AGREE (its SYNTHETIC twin keeps the defective form, so the DISAGREE path is still exercised on a literal); `pointers.py`'s Appendix A line fixture moved 9939 -> 9947, exactly the +8 shift `r3-wl.py` announced for main L >= 9608; `buildtrace.py`'s hard-coded LIVE pair became a read of `method/BUNDLES.tsv`, so no future build can stale it. **A fixture that moves because a repair moved the corpus is not a finding; a fixture that moves for any other reason is.**
