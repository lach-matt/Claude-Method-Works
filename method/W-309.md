### W-309 — `r4-a7.py` seated, and two instruments left held with successors owed

- **THE INSTRUMENT THAT WRITES A BUILD IS A MEMBER OF THE BUILD IT WRITES.** `r4-a7.py` is the seventh of the series and the first to **remove** rows from a reader-facing volume rather than substitute inside one.
- **IT CARRIES A GUARD THE OTHERS DID NOT NEED, AND IT RUNS BEFORE ANY BYTE MOVES**: that **no removal orphans a handle.** Every handle on a removed row is asserted to survive on another row of the same table — `L.def` on Bohr 1913 and Pauli 1925, **`L.bits` on Birkhoff 1937**, `Q.delta` on Pauli 1925, Fermi 1928 and Seaton 1958, `T.trad` on Freuder 1982, `G.book` on Rota 1964. A removal that silently dropped a pointer would be a worse fault than the row it removed.
- **AND ITS DOCSTRING CARRIES THE PROOF**, with the generator's path, the register data it reads and the seven matched spans, so the finding can be re-derived from the store without a network and without re-reading a chat export.
- **TWO INSTRUMENTS ARE HELD, BOTH FOR ONE REASON, AND NEITHER IS EDITED.** `r2-28a4` (W-306) pins *"the sentence prints 162 works"* and *"DATA rows … 162"*; `r2-bib2` (W-308) pins *"the table prints 162 data rows, as the volume states"* and **exits nonzero** on it. **Successors `r2-28a5` and `r2-bib3` are owed.** Held with them: `r2-26c2`, whose successor `r2-26c3` has been owed since W-260, and the six readings and re-takes `gate_live.py --list` names.
- **THE LIVE SET IS NOW 83 GOLDENS**, and the verdict at BUILD129/BUILD303 is **85 OK / 0 FAIL** counting the census step twice — once standalone, once in the walk.

