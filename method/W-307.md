### W-307 — the census closed on the bibliography repair: nothing new, and seven rows gone for one reason

- **0 NEW, 7 GONE, and every one of the seven is a census row on a row this build removed.** `C13-HANDLE-LEAK` ids **431** (`L.def`, the 1913 Pauli row), **493** (`L.bits`, 1937 Shannon), **534** (`Q.delta`, 1958 Pauli), **553** (`G.book`, 1964 Moebius), **609** (`T.trad`, 1982 Racah), and **646** and **649** (`A.orient`, `T.tight`, the 1999 Hentenryck row the merge absorbed).
- **THAT IS THE TIGHTEST CONFIRMATION THIS REPAIR COULD HAVE.** The census names the class the bibliography's objects column belongs to — the standing `C13-HANDLE-LEAK` block of 286 rows closed by `r2-bib` as one class defect — and the seven that left are **exactly** the rows the six substitutions touched, no more and no fewer. **Nothing new appeared anywhere in either bundle.**
- Two rows matched by run and five by item; the by-item rows are the line-number shifts a removal makes in the block below it, and the by-run pair are the same rows read from the regenerated file.
- **THE TOOL REFUSED THE CLOSE AND WAS RIGHT TO.** `close_census2.py` stops on *"a row is never dropped by a regeneration"*: retirement is a ruling, never the tool's, `--retire` is typed and the W text says why. **M ruled it** — *"yes please. retire them"* — and the seven are retired here, each carried verbatim with the mark `RETIRED W-307`. The only precedent, census id 1610 at W-243, was retired the same way.
- **WHAT RETIREMENT MEANS HERE, PRECISELY.** The defect each row recorded is not withdrawn and not re-measured: **the line it sat on no longer exists**, because M approved its removal. A row retired for that reason is a row whose site is gone, and the store keeps it rather than deleting it so the record of what was measured survives the repair. **If the same defect ever measures again it is a NEW row with a new id**, which is the tool's own rule and the reason a retirement can be safe.
- **THE CENSUS CLOSES BEFORE THE GOLDEN RE-BANK**, which is W-274's order.

