### W-300 — the six goldens the relabel moved, re-banked by running

- **THE DIFF CENSUS CAME FIRST AND EVERY ONE OF THE SIX IS DECLARED**, so none is re-banked on trust. `r2-21a2`, `r2-23a2`, `r2-24a`, `r2-scf` and `r2-bib2` each differ in **exactly one line, the Spectra Compendium's md5** — `389c8e05` → `1f328e0b`, its line count unchanged at 1,160 — which is the member W-299's sixty-six substitutions moved and nothing else. `r2-ch20a3` differs in **exactly one line**, its Ne I row listing, which now prints `2s2.2p5.(2P*<1/2>).nd 2[3/2]* J=1`, `2s2.2p5.(2P*<1/2>).ns 2[1/2]* J=1` and `2s.2p6.(2S).np 1P* J=1` where it printed the bare labels.
- **NOT ONE INSTRUMENT REPORTS A CHANGED MEASUREMENT.** Every count, ratio, threshold and check holds across the relabel — including `r2-ch20a3`'s flagged-by-threshold test, **the test that refused the wide relabel** by finding four rows outside its printed five. Under the narrow relabel it finds none. That is the measured difference between the option M took and the one M declined.
- **RE-BANKED BY RUNNING**, under `close_rebank.py`'s guards, with `method/bin` first on PATH.
- **THE CENSUS CLOSED BEFORE THIS BUILD AND NEEDED NO CLOSE**, which is the order W-274 requires: regenerated over BUILD294 it reads 1,636 exact, 0 NEW, 0 GONE, md5 equal to the seated member. No census row quotes a relabelled line.

