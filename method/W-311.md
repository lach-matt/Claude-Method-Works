### W-311 — register 1880's owed expansion is not owed, and a search shaped to find an absence found one

- **M RULED THE APPEND AFTER THE OBJECT WAS FOUND ALREADY PRINTED.** Register 1880 closes *"the Mathematical Compendium prints no object for the bracket system — an expansion owed there and not written here"*. The compendium prints **`### The tower's two ends joined`**, family **K**, at `The_Method_1_6___Mathematical_Compendium-2.md` **L2135–L2148**, whose headline carries the gap-free monotone interval over all **199,130** cells, the directed system **χ(Λ₁₃) → … → χ(Λ₈)**, **slack ≤ 2 rank units**, **ranks 3 through 20** covered, the 1D chain as **terminal object**, the 14D direction as **limit**, and the map form **refuted, branching 4, 4, 6, 8, 9**. Its grade line reads *"Proved — M §12.11.0.11, on the rebuilt tower and its bracket system."*
- **AND IT WAS THERE FIRST.** `tools/buildtrace.py --first "The tower's two ends joined" --stream compendia` reports **BUILD77**, with BUILD75 absent; it is present at **BUILD150**, at **BUILD179** — the last archived compendia build — and in the live bundle. **Register 1880 was seated at main BUILD125, when the compendia bundle stood at BUILD268.** The clause was not overtaken by later work: it was untrue when written.
- **WHY IT SURVIVED IS THE PART WORTH KEEPING, AND IT IS THE SAME FAULT A THIRD TIME.** `compendia2.py`'s `bracket_system()` tested the claim and confirmed it, by searching object **headings in families B and T** for `stage-bracket|bracket system|rank value|fibre`. **The object is in family K**, outside the search, and it is **named for its result rather than its mechanism**, so it matches none of the four words. **The test sought the object by the name it would have borne had it been absent**, and a search shaped to find an absence found one. After `compendia3.py`'s dotted parent regex — 14 where the figure was 73 — and register 1736's author-and-year match, which cannot see a wrong year on a real author, this is the third instance of M's correction landing inside an instrument rather than inside a question.
- **NO VOLUME BYTE MOVES IN THIS BUILD**, because nothing in the volumes is wrong. **A Register entry is never edited**, so 1880 stands and **register 1892 supersedes its closing clause alone** — its measurements are the object's own and its refutation of register 334's map form is untouched.
- **`r4-a8.py` IS THE EIGHTH BUILD INSTRUMENT AND THE FIRST TO CARRY EMPTY SUBSTITUTION SETS.** `MSUBS` and `CSUBS` are both empty by construction: the build appends one Register entry, re-takes the counts that follow from it, and appends this text. Every other guard is its predecessors' — the counts taken from the seated tools on the members as they will stand, the front matter proved to sum to itself, `build.py`'s SUBS anchors re-checked, and each bundle reverse-guarded to its own predecessor's md5.

