### W-308 — the sixty-six goldens one inserted table row moved, re-banked by running

- **THE DIFF CENSUS CAME FIRST AND THE CAUSE IS ONE LINE.** Entry 1891 cites register 1736, which crossed to **seven** citations, so the Register's front-matter table of entries cited seven times or more gained a row **at Register line 59** — and **every Register line below it shifted by one.** `tools/shiftcheck2.py --shift 59:1:7005` **EXPLAINS 27 of the live set outright**, reducing every changed line to its banked form.
- **THE OTHER 39 RESIDUES ARE EACH A DECLARED COUNT, AND EVERY ONE WAS READ.** They are: the **Register's own figures** (extent 1890 → 1891, headings 1725 → 1726, `kinds` corrections 217 → 218, member lines **7,000 → 7,005** — four for entry 1891 and one for the table row); the **main volume gaining a line**, 11,904 → 11,905, because the References sentence now needs a fourth line for the two prior counts; the **Mathematical Compendium's line numbers falling by up to six** as the six rows came out; the **bibliography's own figures** — 162 → 156, *Fifty-two* → *Fifty-one*; and the **citation counts** entry 1891 makes.
- **TWO RESIDUES ARE NOT NOISE BUT INDEPENDENT CONFIRMATION OF THE REPAIR.** `r2-ch27a4` reports the handle `T.trad` as `{'main': 1, 'reg': 1, 'mc': 2}` before and `{'main': 1, 'reg': 2, 'mc': 1}` after — **the compendium's second `T.trad` is the Racah row that went, and the Register's second is entry 1891 naming it.** And `r2-ch19b2` puts Birkhoff's Register lines at 10 before and 11 after, because entry 1891 names Birkhoff 1937 — **the year the removed Shannon row had taken.** The census had already said the same thing from the other side: 0 NEW, 7 GONE, every GONE a row on a line the repair removed.
- **`r2-bib2` IS HELD WITH `r2-bib3` OWED, AND IT IS THE SECOND OF ITS KIND THIS PASS.** Its integrity check pins *"the table prints 162 data rows, as the volume states"* and **it exits nonzero** on that check, so the gate reports `ERROR` rather than a diff. `r2-28a4` is held for the same reason under W-306. **Neither is re-banked**: a re-bank runs the instrument, and running one that reports its own pinned figure as failed would bank that failure as the record.
- **SIXTY-SIX RE-BANKED BY RUNNING**, under `close_rebank.py`'s guards, with `method/bin` first on PATH. **The census closed at BUILD302, before this build**, which is W-274's order.

