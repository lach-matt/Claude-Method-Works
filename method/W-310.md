### W-310 — the two successors the bibliography repair owed, seated

- **BOTH WERE HELD FOR ONE REASON AND NEITHER WAS EDITED.** `r2-28a4` and `r2-bib2` pin the bibliography's extent as an **integrity check** — *"the sentence prints 162 works"*, *"DATA rows … 162"*, *"the table prints 162 data rows, as the volume states"* — and register 1891 restates it at 156. `r2-bib2` **exits nonzero** on its check, so the gate reported `ERROR`; `r2-28a4`'s checks record rather than raise, so it would have re-banked carrying its own `EXPECTED 162` lines. **Banking an instrument's self-reported failure is the same fault as banking a traceback**, and both were held instead.
- **`r2-28a5.py` RE-TAKES FOUR FIGURES AND EXPLAINS EACH.** The three extent pins move to **156**, and the sentence's own figure is read as data, not asserted: the author-and-year match under **Convention AY** is **51** on the 156 rows where it was 52 on the 162, **because the two merged rows were both matches and became one, and not one of the five removed rows was a match.** Three further integrity pins moved and each is accounted rather than bumped: `r2-ch28a`'s surname bound **16 → 17**, because the merged `Deville, Barette & Van Hentenryck` yields a surname where the two single-name rows did not; rows yielding **no** surname **123 → 116**, which is exactly 123 − 5 − 2; and rows whose author is on both lists at another year **18 → 16**, **which is the very class register 1891 removed**. And `WORDS` gained `fifty-one`: the predecessor's word table could not read the figure the repair printed.
- **`r2-bib3.py` TURNS A BROKEN UNIVERSAL INTO A SPLIT THAT NAMES ITS EXCEPTIONS.** `r2-bib2` asserted *"every one of the 286 lines still carries its handle"*, which is now false at seven. The successor asserts instead that **exactly ids 431, 493, 534, 553, 609, 646 and 649 carry the `RETIRED W-307` mark** and that **all 279 others still point at a line that carries the handle**. **A universal with one exception is worth less than a split that names it**, and the seven are the rows M ruled retired.
- **THE FINDING NEITHER SUCCESSOR TOUCHES.** `bib-01` stands — the `objects` column prints handles a reader cannot follow to an object the volume states — and so do `28a-08` and `28a-09`, the surname-overlap bound and the Janet 1928/1929 row that falsifies register 1736's *"never listed"* at one site. **Register 1891 removed rows whose (name, year) no object attributes; it did not touch a handle**, which is why every handle survived on the row that earned it.
- **`gate_live.py`'s HELD entries are removed rather than amended**: a golden with a seated successor needs no entry, because the name rule finds it. **The live set returns to 85.**

