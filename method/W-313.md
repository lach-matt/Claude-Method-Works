### W-313 — `r4-a8.py` seated, the eighth build instrument and the first that moves no volume

- **IT IS THE FIRST OF THE SERIES WITH EMPTY SUBSTITUTION SETS BY CONSTRUCTION.** `MSUBS` and `CSUBS` are both empty: the build appends **one Register entry**, re-takes the counts that follow from it, and appends its own W text. **Nothing in the volumes was wrong, so nothing in the volumes moved** — and the instrument still runs every guard its predecessors do, because a build that changes nothing must prove it changed nothing.
- **AND IT REFUSES A TABLE ROW IT DOES NOT NEED.** `r4-a7.py` had to hand-write a row into the front matter's ≥ 7 table when register 1736 crossed to seven citations. This build's entry cites **334 and 1880**, and neither crosses, so the assertion is the opposite one: **the ≥ 7 set must be unchanged, and the build refuses if it moved.** The carried-over `NEW_ROWS` from its predecessor was removed rather than left inert — a declaration that no longer declares anything is a trap for the next reader.
- **THE COUNTS STILL MOVE AND ARE STILL RE-TAKEN IN THE SAME BUILD**: extent 1891 → 1892, entries 1,733 → 1,734, `kinds` headings 1,663 → 1,664, and the main volume's three sentences that state them.
- **THE CENSUS NEEDED NO CLOSE**, which is the clean case W-274's order exists for: regenerated over BUILD306 it reads **1,629 rows exact, 0 NEW, 0 GONE**, md5 equal to the seated member, because no volume byte moved.
- **MEASURED at BUILD130/BUILD307 before this seating: verify OK, 86 OK / 0 FAIL.**

